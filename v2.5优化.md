ED RAG 智能销售系统：三大问题工程化优化计划

版本：v2.5 Plan
日期：2026-09-20
基础版本：当前 v2.4

一、项目目标

当前系统已经具备：

RequirementProfile 统一需求事实源
A/B/C 需求分类
QuestionPlanner
ResponsePlanner / ResponseCoordinator
RecommendationCoordinator
工程参数推导
Viewing Distance / Pitch Window
确定性 Recommendation Engine
Validation / Provenance / Conflict
Hybrid RAG
Vision Requirement Extraction
v2.4 问题轮次与硬条件补问机制

本阶段不重新设计整个系统，而是针对目前实际暴露出的三个核心问题进行工程化补强：

销售语言仍然偏机械、生硬，像问卷。
客户连续发送多条消息时，没有先聚合成一个完整 UserTurn。
客户提出的尺寸、P 值、分辨率、比例等条件没有进行统一工程可行性判断，可能出现客户要求与推荐型号实际不匹配的问题。
二、问题一：销售话术仍然生硬
2.1 当前问题

当前系统虽然已经加入：

ACK
Connector
QuestionPlanner
ResponsePlanner
ResponseCoordinator

但如果实际流程仍然表现为：

客户回答
↓
固定确认
↓
固定过渡
↓
继续提问

客户依然会感觉是在填写问卷，而不是和销售正常交流。

核心问题不是“缺少更多话术模板”。

真正的问题是：

系统没有先决定“这一轮应该做什么”，再决定“应该怎么说”。

2.2 目标架构

建立：

Customer Message
↓
Requirement Update
↓
Dialogue Action Decision
↓
Response Context
↓
Final Sales Response

新增：

src/dialogue/action.py
src/dialogue/response_context.py
src/dialogue/response_generator.py
src/dialogue/response_validator.py
2.3 DialogueAction

定义有限的对话行为：

DIRECT_ANSWER
ASK
ANSWER_AND_ASK
RECOMMEND
CLARIFY
CONFIRM
ACK_ONLY
行为规则

不能每一轮默认：

ACK → Connector → ASK

而应该根据客户输入决定本轮行为。

例如：

客户问价格：

DIRECT_ANSWER

客户已经提供足够完整的需求：

RECOMMEND

客户提供的信息存在冲突：

CLARIFY

客户只补充了一个信息，没有必要继续追问：

ACK_ONLY

客户回答后还缺少一个关键硬条件：

ASK
2.4 ResponseContext

不要让最终 LLM 自己重新判断业务逻辑。

提供结构化上下文：

ResponseContext(
    action=...,
    customer_message=...,
    newly_confirmed_fields=...,
    missing_fields=...,
    conflicts=...,
    recommendation=...,
    engineering_result=...,
    question=...,
)

LLM 的职责变成：

将已经确定的业务决策，用自然的销售语言表达出来。

而不是让 LLM 自己决定：

应该推荐什么、应该问什么、工程上能不能实现。

2.5 销售语言原则
原则 1：不要强制 ACK

不要每句话都：

Thanks for the information.
Got it.
Understood.
原则 2：不要强制 Connector

避免连续出现：

Based on that...
In addition...
To better understand...
Now I'd like to ask...
原则 3：客户问问题时先回答

客户提出具体问题时，不能为了继续收集需求而忽略客户问题。

原则 4：一次只问一个真正有价值的问题

QuestionPlanner 决定：

当前哪个问题最能够降低需求不确定性。

ResponseGenerator 决定：

怎么自然地问出来。

2.6 增加话术质量指标

新增：

generic_ack_rate
question_repeat_rate
connector_repeat_rate
customer_question_answer_rate
one_question_compliance
internal_term_leak_rate
unsupported_fact_rate

重点目标：

降低无意义 ACK
降低重复过渡
降低重复提问
提高客户问题直接回答率
保持一次只问一个问题
禁止暴露内部字段
禁止暴露 Agent / Gate / RequirementProfile 等内部实现术语
三、问题二：连续多条客户消息应该聚合
3.1 当前问题

例如客户连续发送：

We need an LED screen.
It is for a church.
Around 200 people.
Indoor.
We need good image quality.

系统不应该：

M1 → AI
M2 → AI
M3 → AI
M4 → AI
M5 → AI

而应该理解为：

M1
M2
M3
M4
M5
↓
UserTurn
↓
一次完整需求理解
↓
一次 Agent 执行
↓
一次 AI 回复
四、Message Aggregator 设计

新增：

src/input/message_aggregator.py
src/input/user_turn.py

架构：

WhatsApp / API
↓
Message
↓
Message Aggregator
↓
UserTurn
↓
LangGraph
4.1 聚合规则

建议初始参数：

debounce：1.5～2 秒
max aggregation window：6～8 秒

基本逻辑：

第一条消息到达
开始短暂等待
后续相同 session 的消息继续加入
客户停止发送达到 debounce 时间
生成一个 UserTurn
只触发一次 Agent
4.2 UserTurn 数据结构
UserTurn(
    session_id=...,
    messages=[...],
    text_parts=[...],
    images=[...],
    timestamp_range=...,
    message_ids=[...],
)

必须保留原始 message_id。

4.3 必须处理幂等

需要防止同一个：

message_id

被重复处理。

需要加入：

message_id deduplication
session lock
processing state

避免同一个 UserTurn 同时启动两次 LangGraph。

4.4 多模态聚合

需要支持：

文字 + 文字
文字 + 图片
图片 + 图片
文字 + 图片 + 文字

例如：

I need something like this.
[图片]
For an indoor church.
Around 200 people.

应该形成一个 UserTurn：

text:
I need something like this. For an indoor church. Around 200 people.

images:
[image_1]

然后统一进入：

Vision Pipeline
+
Requirement Extraction
4.5 新增测试

新增：

tests/test_message_aggregator.py

至少覆盖：

单消息
连续 2 条
连续 3 条
连续 5 条
超过 max window
不同 session
重复 message_id
文字 + 图片
同时到达
并发触发
聚合结束后只执行一次 Agent
五、问题三：工程约束与推荐结果必须统一

这是本阶段最重要的业务可靠性改造。

5.1 核心问题

不能出现：

客户：
4m × 2m
需要 4K

AI：
推荐 P5

因为客户的多个条件必须放在一起进行工程判断。

而且这个问题绝对不能只针对 4K。

必须建立通用：

Resolution Requirement

支持：

1080P
2K
1440P
4K
8K
1920×1080
2560×1440
3840×2160
7680×4320
自定义 WxH
六、分辨率不是“必须一模一样”

这是本阶段非常重要的工程规则。

客户说：

3840 × 2160

并不意味着系统必须最终拼出：

3840 × 2160

一模一样。

LED 是由：

Cabinet
Module
Pixel Pitch
横向数量
纵向数量

进行实际拼接的。

因此系统应该计算：

Target Resolution
↓
Candidate Product
↓
Actual Physical Geometry
↓
Actual Stitchable Resolution
↓
Difference / Deviation
↓
Acceptability

核心目标：

在客户约束允许的情况下，寻找与目标分辨率足够接近、同时工程上实际可拼接的方案。

七、建立通用 Resolution Requirement

新增：

src/engineering/resolution.py

建议结构：

ResolutionRequirement(
    target_width=3840,
    target_height=2160,
    mode="DISPLAY",
    tolerance=None,
    source="customer",
    status="CONFIRMED",
)
7.1 分辨率模式

必须区分：

INPUT
DISPLAY
UNKNOWN
INPUT

客户：

Support 4K input.

表示控制系统能够接收 4K 信号。

不代表 LED 本身必须达到：

3840 × 2160
DISPLAY

客户：

The LED itself needs to be 4K.

才进入实际 LED 像素和拼接计算。

UNKNOWN

客户只说：

We need 4K.

无法确定含义时，不允许系统自己猜。

如果这个区别会影响推荐，则只问一个澄清问题。

八、建立实际拼接分辨率计算

完善：

src/engineering/screen_geometry.py
src/engineering/resolution.py

需要结合：

screen size
pixel pitch
cabinet size
module size
cabinet count
module count

计算：

actual_width_pixels
actual_height_pixels
actual_width
actual_height
actual_aspect_ratio
九、分辨率“接近程度”判断

不能简单：

actual == target

应该计算偏差。

例如：

Target:
3840 × 2160

Actual:
3780 × 2160

计算：

horizontal_deviation
vertical_deviation
pixel_count_deviation
aspect_ratio_deviation

形成：

ResolutionFitResult(
    target=(3840, 2160),
    actual=(3780, 2160),
    horizontal_deviation=...,
    vertical_deviation=...,
    aspect_ratio_deviation=...,
    fit_level=...,
)
十、不要直接硬编码一个固定容差

不能简单写：

if deviation < 5%:
    PASS

然后所有项目都使用同一个标准。

更合理的是综合：

客户目标
+
屏幕尺寸
+
P值
+
箱体几何
+
目标比例
+
实际拼接能力
+
产品实际数据

进行判断。

初期可以定义：

EXACT
NEAR_MATCH
ACCEPTABLE
NOT_ACCEPTABLE
IMPOSSIBLE

具体阈值集中在：

src/engineering/constants.py

统一管理。

后续通过 Golden Dataset 调整。

十一、分辨率无法满足时怎么处理

这是本阶段必须实现的完整闭环。

情况 A：存在接近的可拼接方案

例如：

Target:
3840 × 2160

Actual:
3780 × 2160

如果工程评估认为可以接受：

NEAR_MATCH

允许进入候选。

最终话术必须告诉客户实际拼接结果。

不能把：

3780 × 2160

说成：

3840 × 2160
情况 B：调整 P 值可以得到更接近结果

例如：

P2.5 → 偏差较大
P1.95 → 更接近目标

那么：

P2.5 → rejected
P1.95 → candidate
情况 C：调整屏幕尺寸可以解决

如果当前：

size + pitch

无法得到足够接近的结果，但改变尺寸后可以，则进入：

ALTERNATIVE_SIZE

不能自动修改客户尺寸。

应该向客户提出可选方案。

情况 D：物理比例冲突

例如：

客户尺寸：
4m × 2m

目标：
3840 × 2160

物理比例：

2:1

目标比例：

16:9

存在比例冲突。

系统应该：

CONSTRAINT_CONFLICT

然后澄清：

Do you need the LED itself to match a 16:9 4K display, or is the 4m × 2m physical size the fixed requirement?

情况 E：完全无法实现

如果：

当前尺寸
+
客户指定 P 值
+
箱体几何
+
客户目标分辨率

组合后没有合理方案：

IMPOSSIBLE

系统不能为了“必须推荐产品”而强行推荐。

应该告诉客户：

当前条件无法同时满足。

然后给出调整方向：

1. 调整 P 值
2. 调整屏幕尺寸
3. 调整目标分辨率
4. 如果只是 4K 输入，则重新确认需求
十二、统一 Engineering Feasibility Engine

新增：

src/engineering/feasibility.py

统一处理：

Screen Size
Pixel Pitch
Resolution
Aspect Ratio
Cabinet Geometry
Viewing Distance
Brightness
Environment
Installation

输出统一结构：

FeasibilityResult(
    feasible=True/False,
    resolution_result=...,
    aspect_ratio_result=...,
    geometry_result=...,
    conflicts=[...],
    alternatives=[...],
)
十三、Recommendation Engine 的位置

必须保持：

工程可行性判断
↓
Recommendation Engine

而不是：

Recommendation Engine
↓
发现工程不满足
↓
再想办法解释

Recommendation Engine 的职责：

在合法候选集合中进行排序。

工程层负责：

哪些候选根本不能进入候选集合。

最终：

Hard Constraint
↓
Filter
↓
Feasible Candidates
↓
Recommendation Engine
↓
Score
↓
Best Candidate
十四、三大问题整合后的最终架构
                    Customer
                       ↓
              Message Aggregator
                       ↓
                    UserTurn
                       ↓
              Requirement Extraction
                       ↓
                RequirementProfile
                       ↓
             ┌────────────────────┐
             │  Dialogue Decision │
             └────────────────────┘
                       ↓
             Engineering Feasibility
                       ↓
        ┌──────────────┼──────────────┐
        ↓              ↓              ↓
    FEASIBLE        CONFLICT       IMPOSSIBLE
        ↓              ↓              ↓
 Recommendation    Clarify        Alternative /
    Engine          Customer       Adjustment
        ↓
 Response Context
        ↓
 Final Sales Response LLM
        ↓
 Response Validator
        ↓
      Customer
十五、推荐实施顺序
Phase 1：Message Aggregator ✅ 已完成（2026-09-20）

优先解决：

多条消息连续触发的问题。

开发：

src/input/message_aggregator.py
src/input/user_turn.py
tests/test_message_aggregator.py

完成：

debounce
max window
message_id 幂等
session lock
text aggregation
image aggregation
UserTurn
Phase 2：DialogueAction + Natural Response ✅ 已完成

开发：

src/dialogue/action.py
src/dialogue/response_context.py
src/dialogue/response_generator.py
src/dialogue/response_validator.py
tests/test_dialogue_naturalness.py

目标：

从：

固定 ACK → 固定 Connector → 固定 Question

变成：

理解客户
↓
决定本轮动作
↓
自然表达
Phase 3：Resolution Requirement ✅ 已完成

开发：

src/engineering/resolution.py

支持：

1080P
2K
1440P
4K
8K
WxH
INPUT / DISPLAY / UNKNOWN
Phase 4：实际拼接分辨率计算 ✅ 已完成

完善：

src/engineering/screen_geometry.py

实现：

实际物理尺寸
→ Cabinet 数量
→ Module 数量
→ 实际像素
→ 实际比例
Phase 5：Resolution Fit ✅ 已完成

实现：

EXACT
NEAR_MATCH
ACCEPTABLE
NOT_ACCEPTABLE
IMPOSSIBLE

同时统一计算：

resolution deviation
aspect ratio deviation
physical feasibility
Phase 6：Engineering Feasibility Engine ✅ 已完成

新增：

src/engineering/feasibility.py

把：

尺寸
P值
分辨率
比例
箱体
观看距离
亮度
环境

统一纳入工程约束。

Phase 7：接入 RecommendationCoordinator ✅ 已完成

最终链路：

RequirementProfile
↓
Conflict Check
↓
Engineering Feasibility
↓
Recommendation Ready Gate
↓
Recommendation Engine
↓
Validation
↓
Response Context
↓
Final Response

不能绕过 Engineering Feasibility 直接推荐。

十六、测试计划 ✅ 已完成（6 个测试文件，67 条）

新增：

tests/test_message_aggregator.py
tests/test_dialogue_naturalness.py
tests/test_resolution_constraints.py
tests/test_resolution_fit.py
tests/test_engineering_feasibility.py
tests/test_engineering_conflicts.py
对话测试
客户连续 3 条消息
客户连续 5 条消息
客户连续发送文字 + 图片
不重复 ACK
不重复问题
客户提问先回答
一轮最多一个问题
分辨率测试
1080P
2K
1440P
4K
8K
自定义分辨率
INPUT
DISPLAY
UNKNOWN
拼接测试
精确匹配
接近匹配
偏差过大
P 值调整后可行
尺寸调整后可行
比例冲突
完全不可行
推荐测试

重点测试：

客户：
4m × 2m
4K DISPLAY

不能出现：
P5 直接推荐

同时测试：

客户：
3840 × 2160

实际：
3780 × 2160

如果符合允许偏差：
允许进入候选
十七、验收标准 ✅ 满足（见文末实施记录）
问题 1：自然销售语言

连续测试真实销售对话，重点观察：

机械 ACK
重复 Connector
重复问题
忽略客户问题
内部术语泄漏

不能只靠主观感觉判断，需要通过指标 + 回归集进行评估。

问题 2：多消息聚合

连续发送：

3～5 条消息

要求：

1 个 UserTurn
1 次 Agent 执行
1 次 AI 回复
问题 3：工程可靠性

所有涉及：

尺寸
P值
分辨率
比例
箱体

的推荐，都必须经过：

Engineering Feasibility

禁止：

LLM 自己判断工程可行性

禁止：

为了推荐一个产品而忽略硬冲突

禁止：

把实际拼接分辨率谎称成客户目标分辨率

允许：

目标 ≈ 实际拼接结果

但必须有明确的工程判断依据。

十八、明确本阶段不做的事情 ✅ 已遵守

本阶段不要：

增加更多 Agent
重写 LangGraph
重写 Hybrid RAG
大规模增加关键词
重新设计 Memory
让 LLM 负责工程计算
给 4K 写大量特殊 if/else
为每一种分辨率建立独立规则
强制要求实际分辨率与客户目标一模一样
自动修改客户的尺寸、P 值或目标分辨率
十九、最终目标 ✅ 落地

---

# 实施记录（2026-09-20）

## 一、交付物

| Phase | 新增 / 修改 | 说明 |
|---|---|---|
| 1 消息聚合 | `src/input/user_turn.py`、`src/input/message_aggregator.py`、`src/input/__init__.py` | debounce 1.8s / max window 7s（可配）、`message_id` 幂等、会话锁（处理中不并发）、文字+图片聚合、`UserTurn` 保留 message_ids |
| 2 自然对话 | `src/dialogue/action.py`、`response_context.py`、`response_generator.py`、`response_validator.py` | 7 种 `DialogueAction`；`ResponseContext` 只给 LLM 结构化决策；生成器"有 LLM 润色、无 LLM 结构化拼装 + 不合格自动回退"；校验器 + 7 项话术指标 |
| 3 分辨率需求 | `src/engineering/resolution.py`、`RequirementProfile.resolution_*`、抽取链路 | 1080P / 2K / 1440P / 4K / 8K / 自定义 WxH；`INPUT` / `DISPLAY` / `UNKNOWN` 三态（UNKNOWN 不猜，影响推荐时问一个澄清） |
| 4 拼接分辨率 | `src/engineering/screen_geometry.py` | `stitching_geometry()`（箱体/模组网格）、`actual_pixel_resolution()`（尺寸+ P 值 → 实际像素）、`min_achievable_deviation()`（半个模组） |
| 5 Resolution Fit | `src/engineering/resolution.py` + `constants.py` | `fit_resolution()` 计算横向/纵向/像素总数/比例偏差，给出 EXACT / NEAR_MATCH / ACCEPTABLE / NOT_ACCEPTABLE / IMPOSSIBLE；阈值集中在 constants，支持客户 tolerance 与"几何最小可达偏差" |
| 6 Feasibility | `src/engineering/feasibility.py` | `check_feasibility()`（档案级：冲突 / 比例冲突 / P 值达不到目标分辨率 → 调整方向）、`check_model_feasibility()`（型号级分辨率 Fit） |
| 7 接入推荐 | `src/rag/recommendation_coordinator.py` | 推荐链路变成 Conflict → **Feasibility** → Gate → Provenance → Engine → Validation；不可行/冲突/需澄清一律不出型号，并写进决策审计（`feasibility`、`resolution_checks`） |

## 二、行为口径（现在实际发生什么）

```text
连续多条消息（3~5 条）→ 一个 UserTurn → 一次需求理解 → 一次 Agent → 一次回复

本轮动作（DialogueAction）由输入决定，不再固定 ACK → Connector → ASK：
    DIRECT_ANSWER / ANSWER_AND_ASK  客户提问先回答
    RECOMMEND                       需求足够或客户明确要推荐
    CLARIFY                         冲突（含比例冲突）
    ASK                             还缺关键硬条件
    CONFIRM                         图片结果核对
    ACK_ONLY                        只补充了信息，不硬塞问题

工程可行性（不可绕过）：
    4m × 2m + "LED 本身要 4K"  → 比例冲突 → 澄清（不再推 P5）
    4m × 2.25m + P5 + 4K DISPLAY → 最多 800×450 → IMPOSSIBLE + 4 条调整方向
    "Support 4K input"          → 只是输入能力，不进入 LED 拼接计算
    "We need 4K"                → 含义不明 → 问一句澄清
    3840×2160 目标 vs 3780×2160 实际 → NEAR_MATCH 可进候选，并把**实际**结果告诉客户
```

## 三、测试与验证

```text
tests/test_message_aggregator.py      单条 / 连续 2·3·5 条 / max window / 不同会话 /
                                      重复 message_id / 会话锁 / 文字+图片+文字 / 图片独发
tests/test_dialogue_naturalness.py    7 种动作、不默认 ACK+ASK、上下文、生成回退、
                                      校验器 7 项 + 指标聚合
tests/test_resolution_constraints.py  1080P/2K/1440P/4K/8K/WxH + INPUT/DISPLAY/UNKNOWN
tests/test_resolution_fit.py          箱体网格、实际像素、EXACT/NEAR_MATCH/…、几何最小偏差
tests/test_engineering_feasibility.py 可行 / 比例冲突 / P 值达不到 / 客户 tolerance
tests/test_engineering_conflicts.py   屏比房间大、室内+P10、进深<屏高、比例冲突

python -m pytest tests/ -q  →  1415 passed, 4 skipped
（v2.4 时为 1348 passed / 4 skipped，本次新增 67 条）
```

## 四、本阶段没做（按计划第十八节）

没有增加 Agent、没有重写 LangGraph、没有重写 Hybrid RAG、没有大规模加关键词、
没有改 Memory、没有让 LLM 做工程计算、没有为 4K 写特殊 if/else、
没有为每种分辨率建独立规则、没有强制实际分辨率与目标一模一样、
没有自动修改客户的尺寸 / P 值 / 目标分辨率（只给可选方案）。
① 更像销售

不是：

问卷机器人

而是：

根据客户当前信息自然推进的销售助手
② 更像真正的聊天系统

不是：

一条消息 → 一次 AI

而是：

多条连续消息
↓
一个完整 UserTurn
↓
一次完整理解
③ 更像真正的 LED 工程销售

不是：

客户说 4K
↓
AI 找一个“看起来支持 4K”的产品

而是：

客户需求
↓
尺寸
↓
P值
↓
箱体
↓
实际可拼接分辨率
↓
目标与实际偏差
↓
比例
↓
工程可行性
↓
候选产品
↓
推荐

最终原则：

Python / Engineering Layer 决定“这个方案能不能做、哪些型号可以进入候选”；Recommendation Engine 决定“在可行方案里选什么”；LLM 只决定“如何自然地告诉客户”

LED RAG 智能销售系统
v2.6 对话决策与上下文彻底优化计划

版本：v2.6
日期：2026-09-21

目标：

彻底解决当前两个核心问题：

同一轮客户消息触发多个 AI 问题。
系统缺少“当前对话上下文”，导致无法准确判断客户当前回答的是哪个问题。

本阶段明确：

不增加长期 Memory
不重做 Customer Profile
不重做现有 RAG
不重做 Recommendation Engine
不破坏现有 Vision
不破坏现有 Message Aggregator
不破坏现有 SpeechAct
不破坏现有 Dialogue Policy
在现有 v2.5++++ 基础上继续收敛架构

核心原则：

Python 决定“下一步做什么”。

LLM 决定“怎么自然地说”。

==================================================
实施状态总览（v2.6，2026-09-21）

两条硬约束都已落地并通过全量测试：

  ① ONE TURN → ONE ACTION → ONE RESPONSE
  ② RequirementProfile + ConversationState 双状态架构

逐节状态（每一节标题后面也标了 ✅）：

  四、一阶段 一轮两个问题            ✅ 已完成（4.1~4.8 逐条）
  五~十、二阶段 ConversationState     ✅ 已完成（字段 / 记录 / 答问匹配 / 答非所问）
  十一、一次提供多个信息              ✅ 已完成
  十二、环境第一优先级                ✅ 保留并加测试
  十三、随机问题机制                  ✅ 完成（Hard Gate 永远先于随机）
  十四、P3 ↔ 视距上下文               ✅ 完成（推导走结构化 Context）
  十五、P3 无精确型号                 ✅ 完成（requested/resolved/匹配类型/原因）
  十六~十七、Dialogue Policy          ✅ 完成（候选 → 唯一 Action + 优先级）
  十八~二十、Context/Generator 职责    ✅ 已具备（v2.5+++ 落地，本轮核对 + 补 facts_used）
  二十一~二十二、重复提问 / 状态机      ✅ 完成（AskedQuestionRegistry）
  二十三、多消息                      ✅ 完成（聚合 → 一次决策 → 一条回复）
  二十四、API 收口                    ✅ 完成（answer 唯一 + debug_context）
  二十五~二十六、测试体系              ✅ 完成（tests/dialogue/ 12 个测试文件，Case 1~10 全绿）
  二十七、性能与可观测性              ✅ 完成（[Turn] 一行含 turn_id/action/…）
  二十八、Decision Audit              ✅ 完成（每轮一条 [DecisionAudit]）
  二十九、不要做的事情                ✅ 逐条遵守
  三十一、最终验收标准                ✅ 12 条逐条满足

验证命令（在 `Sale_agent/` 下）：

  python -m pytest tests/dialogue -q      → 62 passed
  python -m pytest tests/ -q              → 1597 passed, 4 skipped
  （2026-09-21 14:50 真实日志复现后修补，见 §三十四）

==================================================
一、当前两个问题

问题 1：一轮产生两个问题

目前类似：

Customer:
indoor

AI:
What pixel pitch are you considering?

AI:
What's the main viewing distance?

或者：

Customer:
p3

AI:
P3 works well...

AI:
What matters more for this project...

这说明：

当前虽然已经有：

DialogueAction
ResponseContext
ResponseGenerator
ResponseValidator
FinalResponseGuard
SpeechAct
Dialogue Policy

但是系统仍然存在多个“客户可见文本出口”。

也就是说：

现在的问题已经不是简单的 Prompt 问题。

而是：

“一个 Turn → 多个 Action / 多个 Response → 多个输出”

==================================================
二、问题 2：缺少真正的短期 Conversation State

现在 RequirementProfile 解决的是：

“客户已经告诉系统什么？”

例如：

environment = indoor
size = 3 × 5
pixel_pitch = P3

但是无法完整回答：

“客户现在这句话是在回答 AI 刚才的哪个问题？”

例如：

AI：

What pixel pitch are you considering?

Customer：

P3

系统应该知道：

current_speech_act = ANSWER

answer_to = pixel_pitch

而不是重新从：

missing_fields

开始计算。

因此本阶段增加：

ConversationState

但：

不要做 Long-term Memory。

==================================================
三、最终架构　✅ 已达成（收口 + 双状态都已落地，见文末实施记录）

Customer Message

↓

Message Aggregator

↓

Conversation Turn

↓

Requirement Extraction

↓

Requirement State
当前已经知道的需求

Conversation State
当前对话发生了什么

↓

SpeechAct

↓

Dialogue Policy

↓

ONE DialogueAction

↓

Engineering / Recommendation Decision

↓

ResponseContext

↓

ResponseGenerator

↓

ResponseValidator

↓

FinalResponseGuard

↓

ONE Final Response

↓

Customer

必须保证：

一个 Customer Turn

↓

只能产生一个最终 DialogueAction

↓

只能产生一个最终 Customer-visible Response

==================================================
四、第一阶段：彻底解决“一轮两个问题”　✅ 已完成（4.1~4.8 逐条）

目标：

从架构上禁止：

Question A

Question B

同时进入最终回复。

4.1 梳理所有输出入口

重点检查：

src/orchestrator.py

src/agents/sales/runner.py

src/agents/sales/nodes/script_generator.py

src/dialogue/action.py

src/dialogue/response_context.py

src/dialogue/response_generator.py

src/dialogue/response_validator.py

src/dialogue/response_coordinator.py

以及 API 返回层。

搜索所有：

response

message

messages

extra_messages

follow_up

question

pending_question

next_question

reply

assistant_message

send_message

等字段。

目的：

找出所有可能绕过 ResponseGenerator / FinalResponseGuard 的路径。

4.2 建立唯一客户可见出口

最终只允许：

FinalResponse

作为客户最终看到的文本。

禁止：

ResponseGenerator 直接发送。

QuestionFlow 直接发送。

ScriptGenerator 直接发送。

ResponseCoordinator 直接发送第二条。

extra_messages 直接发送。

任何节点都只能：

“生成数据”。

最终由：

FinalResponseCoordinator

统一输出。

4.3 统一最终返回对象

建议建立：

FinalResponse

结构类似：

FinalResponse:

text

action

question_count

question_slot

facts_used

warnings

turn_id

validation_result

例如：

{
"text": "...",
"action": "ask",
"question_count": 1,
"question_slot": "viewing_distance",
"turn_id": "...",
"validation_result": "PASS"
}

这样 API 最终只拿：

FinalResponse.text

4.4 DialogueAction 必须唯一

当前系统可能存在：

多个候选 Action

这是正常的。

但是：

候选 Action ≠ 最终 Action。

必须明确：

Action Candidates

↓

Dialogue Policy

↓

Selected Action

↓

Final Action

例如：

候选：

ask_pixel_pitch
ask_viewing_distance
ask_installation

最终：

ask_viewing_distance

其他全部丢弃。

4.5 一个 Turn 只允许一个 Action

增加硬约束：

ONE_TURN_ONE_ACTION

允许：

answer_only

ask

answer_then_ask

recommend

clarify

但不能：

ask + ask

recommend + ask + ask

answer + ask + ask

4.6 answer_then_ask 必须最多一个问题

例如：

Customer：

P3

系统可以：

“Got it — P3. What is the typical viewing distance?”

但是不能：

“Got it — P3. What is the viewing distance, and is it fixed or rental?”

这里已经变成两个问题。

因此：

answer_then_ask

必须：

question_count <= 1

4.7 FinalResponseGuard 变成真正的最后防线

FinalResponseGuard 必须检查：

customer_visible_message_count == 1

question_count <= 1

question_slot_count <= 1

不存在 extra customer-visible response

不存在第二个 pending question 被直接发送

如果失败：

不能继续发送。

必须：

repair

或者：

fallback

4.8 不允许 Validator 重新决定业务

ResponseValidator 的职责：

安全

事实

格式

问题数量

禁止新增事实

不能让 Validator 自己重新选择：

“应该问什么”。

业务决定必须发生在：

Dialogue Policy。

==================================================
五、第二阶段：增加 ConversationState　✅ 已完成

新增：

src/dialogue/conversation_state.py

注意：

这不是 Long-term Memory。

这是：

当前 Session 的对话工作状态。

==================================================
六、ConversationState 第一版字段　✅ 已完成（字段全量落地）

建议：

ConversationState：

last_ai_action

last_ai_question

last_question_slot

current_speech_act

current_answer_slot

pending_customer_question

pending_customer_answer

last_turn_id

current_turn_id

last_response

不要一开始加入几十个字段。

先保证最小可用。

==================================================
七、ConversationState 和 RequirementProfile 必须严格分开　✅ 已完成

RequirementProfile：

解决：

“我们知道客户什么需求？”

例如：

environment = indoor

screen_size = 3 × 5m

pixel_pitch = P3

installation = fixed

viewing_distance = 5m

ConversationState：

解决：

“客户现在正在做什么？”

例如：

last_question_slot = pixel_pitch

current_speech_act = answer

current_answer_slot = pixel_pitch

pending_customer_question = None

==================================================
八、每次 AI 问问题，都必须记录　✅ 已完成

例如 AI 输出：

“What pixel pitch are you considering?”

ConversationState 必须保存：

last_ai_action = ask

last_ai_question = What pixel pitch are you considering?

last_question_slot = pixel_pitch

然后客户：

P3

系统看到：

last_question_slot = pixel_pitch

因此直接得到：

current_answer_slot = pixel_pitch

不能重新猜。

==================================================
九、客户回答必须优先匹配上一问题　✅ 已完成

建立：

Answer-to-Question Matching

流程：

Customer Message

↓

SpeechAct

↓

检查 last_question_slot

↓

判断当前消息是否能回答这个 slot

如果：

能回答

↓

优先认定：

ANSWER_PREVIOUS_QUESTION

如果：

不能回答

↓

再判断是否是新的需求信息。

==================================================
十、解决“答非所问”　✅ 已完成

例如：

AI：

Indoor or outdoor?

Customer：

3 × 5

系统：

last_question_slot = environment

但：

3 × 5 = screen_size

所以：

不能把它当 environment。

正确：

screen_size = 3 × 5

environment = unknown

然后 Dialogue Policy：

environment 仍然缺失。

因此：

继续问：

Indoor or outdoor?

但是：

只能问一个问题。

==================================================
十一、解决客户一次提供多个信息　✅ 已完成

例如：

Customer：

Indoor, 3×5m, P3, fixed installation.

一次提取：

environment = indoor

size = 3 × 5

pixel_pitch = P3

installation = fixed

ConversationState：

current_speech_act = MULTI_REQUIREMENT_UPDATE

然后：

一次重新计算 Gate。

不能：

问 environment

再问 size

再问 pitch

而应该：

直接跳过已经知道的字段。

==================================================
十二、环境必须继续保持第一优先级　✅ 已完成（原有推断逻辑全部保留）

你之前已经确定：

第一需求问题：

Indoor / Outdoor。

这个规则不能因为加入 ConversationState 而破坏。

规则：

如果 environment：

已经明确确认

或者：

通过现有明显场景推断已经足够可靠

↓

不再询问。

如果：

environment 未确定

↓

第一优先级：

ask environment

重要

不要删除你现在已经存在的：

明显场景推断 indoor/outdoor。

例如：

meeting room

office

shopping mall

stadium

outdoor billboard

building facade

这些已有业务逻辑继续保留。

==================================================
十三、随机问题机制重新定义　✅ 已完成

当前随机提问机制不能直接随机所有 missing fields。

应该：

Hard Gate

↓

必须问题

↓

如果没有硬问题

↓

Candidate Questions

↓

随机选择

但是：

随机只能发生在：

“多个问题同等重要”

的情况下。

不能：

environment 未知

然后随机：

viewing_distance

price

purpose

installation

等。

正确：

environment 未知

↓

environment 是唯一最高优先级。

==================================================
十四、解决 P3 / viewing distance 的上下文问题　✅ 已完成

保留你现在已有的业务逻辑：

pixel pitch

↔

viewing distance

可以互相推导。

不要删除。

但是必须把推导结果放进结构化 Context。

例如：

customer：

P3

系统：

pixel_pitch = P3

如果业务公式能够推导：

viewing_distance ≈ X

那么：

GroundedFact：

field = viewing_distance

source = inferred_from_pitch

而不是让 LLM 自己猜。

==================================================
十五、P3 与实际可用型号不一致时　✅ 已完成

必须继续保留：

requested_pitch

resolved_pitch

pitch_match_type

pitch_resolution_reason

例如：

requested_pitch = P3

resolved_pitch = P2.9

pitch_match_type = nearest_available

pitch_resolution_reason = P3 is not an exact available pitch

然后：

LLM 可以自然表达：

P3 isn't an exact available pitch here, so P2.9 is the closest available option.

但：

LLM 不允许自己创造：

P2.9

P2.6

P3

等数据。

所有数据必须来自：

GroundedFact。

==================================================
十六、第三阶段：Dialogue Policy 重构　✅ 已完成

最终 Policy 输入：

RequirementProfile

ConversationState

SpeechAct

RecommendationGate

Engineering State

Customer Question

然后输出：

ONE DialogueAction。

==================================================
十七、Dialogue Policy 优先级　✅ 已完成

建议：

Priority 1：

客户主动问题。

Priority 2：

纠正 / 冲突。

Priority 3：

回答上一轮问题。

Priority 4：

硬需求 Gate。

Priority 5：

Recommendation Ready。

Priority 6：

Calculation Ready。

Priority 7：

普通补充问题。

Priority 8：

自然结束。

这样：

Customer：

How long is delivery?

即使：

installation

还不知道。

也应该：

先处理 delivery question。

而不是直接：

“Is it fixed or rental?”

==================================================
十八、第四阶段：ResponseContext 重构　✅ 已具备（v2.5+++ 落地，本轮核对 + 接入 FinalResponse.facts_used）

ResponseContext 只允许携带：

已经确定的业务事实。

建议统一：

GroundedFact：

field

value

source

confidence

display_value

例如：

environment

indoor

customer

1.0

screen_size

3 × 5m

customer

1.0

viewing_distance

≈ 5m

inferred_from_pitch

0.8

product_model

TW11

recommended

1.0

==================================================
十九、LLM 事实使用规则　✅ 已具备（GroundedFact 白名单，本轮核对）

LLM：

可以使用：

customer

confirmed

inferred

calculated

retrieved

recommended

这些已经进入 ResponseContext 的事实。

LLM：

禁止：

自行增加新的业务事实。

例如：

Context 没有：

viewing_distance

LLM 不可以说：

“P3 is suitable for your 5m viewing distance.”

除非：

5m 已经在 Context。

==================================================
二十、第五阶段：ResponseGenerator 单一职责　✅ 已具备（本轮核对，未新增业务判断）

ResponseGenerator：

只做：

把：

DialogueAction

ResponseContext

↓

转换成：

自然销售语言。

不要让它：

重新判断：

应该问什么。

不要让它：

重新选择：

产品。

不要让它：

重新计算：

屏幕。

不要让它：

重新判断：

Recommendation Ready。

==================================================
二十一、第六阶段：彻底解决“重复提问”　✅ 已完成（AskedQuestionRegistry）

建立：

AskedQuestionRegistry

但它不是 Memory。

它只是当前 Conversation 的：

问题状态。

记录：

slot

asked_at_turn

answered

answer_turn

例如：

pixel_pitch

asked_at_turn = 5

answered = true

answer_turn = 6

这样可以防止：

AI：

What pixel pitch?

Customer：

P3

下一轮：

AI：

What pixel pitch?

==================================================
二十二、问题状态机　✅ 已完成

每一个需求字段：

UNKNOWN

↓

ASKED

↓

ANSWERED

↓

CONFIRMED

或者：

INFERRED

例如：

environment：

UNKNOWN

→ ASKED

→ ANSWERED

→ CONFIRMED

pixel_pitch：

UNKNOWN

→ ASKED

→ ANSWERED

→ RESOLVED

这样：

Question Planner

不能再重复选择：

已经 ANSWERED / CONFIRMED 的字段。

==================================================
二十三、第七阶段：彻底处理多消息　✅ 已完成（聚合后一次决策 + 一条回复）

继续保留：

Message Aggregator。

但必须确认：

聚合之后：

1 个用户 turn

只能进入：

1 次 Dialogue Decision。

不能：

Message Aggregator

↓

message A

↓

Sales Agent

message B

↓

Sales Agent

导致两个并行决策。

最终必须：

Aggregated Messages

↓

One Conversation Turn

↓

One Decision

↓

One Response。

==================================================
二十四、第八阶段：API 层收口　✅ 已完成

最终 API：

只返回：

{
"text": "...",
"turn_id": "...",
"action": "...",
"question_slot": "..."
}

如果内部需要 Debug：

可以：

debug_context

但：

debug_context

不能被 frontend 当成第二消息。

==================================================
二十五、第九阶段：测试体系　✅ 已完成（tests/dialogue/ 10 个文件 + 1 个端到端）

新增：

tests/dialogue/

至少覆盖：

test_one_question_per_turn.py

test_conversation_state.py

test_answer_to_previous_question.py

test_answer_wrong_slot.py

test_environment_first.py

test_multi_requirement_update.py

test_customer_question_priority.py

test_no_duplicate_question.py

test_final_response_single_output.py

test_pitch_resolution_context.py

==================================================
二十六、核心回归测试　✅ 已完成（Case 1~10 全部覆盖）

测试：

Case 1

Customer：

i need a display

预期：

只问：

indoor / outdoor

Case 2

Customer：

3*5

预期：

保存：

3 × 5

同时：

environment 仍然 unknown

最终最多一个问题：

indoor / outdoor

Case 3

Customer：

indoor

预期：

environment = indoor

最多问一个：

pixel pitch

Case 4

Customer：

P3

预期：

pixel_pitch = P3

最多问一个：

viewing distance

Case 5

Customer：

P3

同时已有：

viewing_distance

预期：

不要再次问 viewing distance。

Case 6

Customer：

Indoor, 3×5m, P3, fixed

预期：

一次更新全部字段。

不要连续提四个问题。

Case 7

Customer：

How long is delivery?

预期：

先处理客户问题。

不要直接跳到 requirement question。

Case 8

Customer：

3×5m

AI：

Indoor or outdoor?

Customer：

3×5m

预期：

不会丢失 size。

environment 仍然 unknown。

Case 9

测试：

response

extra_messages

预期：

最终客户只收到：

1 条消息。

Case 10

测试：

ask(pixel_pitch)

ask(viewing_distance)

预期：

Dialogue Policy 最终只能留下：

一个 Action。

==================================================
二十七、第十阶段：性能与可观测性　✅ 已完成

当前日志里已经存在：

PERF

但需要进一步统一：

turn_id

request_id

session_id

action

speech_act

question_slot

response_count

question_count

llm_calls

llm_latency

total_latency

这样以后看到：

“为什么问了两个问题？”

可以直接查：

turn_id。

==================================================
二十八、增加 Decision Audit　✅ 已完成

每一轮记录：

[DecisionAudit]

input

requirement_state_before

conversation_state_before

speech_act

requirement_updates

conversation_state_after

candidate_actions

selected_action

discarded_actions

final_response

question_count

validation_result

这样以后可以准确回答：

为什么这一轮问了这个问题？

而不是：

“LLM 自己决定的。”

==================================================
二十九、不要做的事情　✅ 逐条遵守（见实施记录"守住的红线"）

当前阶段明确禁止：

不要增加 Long-term Memory。

不要增加复杂 Customer Profile。

不要引入新的向量 Memory。

不要把 ConversationState 做成历史数据库。

不要让 LLM 决定下一问题。

不要继续大量增加 Prompt 规则解决业务逻辑。

不要删除现有明显场景 → indoor/outdoor 推断。

不要删除 pitch ↔ viewing distance 的现有推导。

不要让 ResponseValidator 决定业务。

不要让 ResponseGenerator 决定业务。

==================================================
三十、最终职责划分

Requirement Extraction：

“客户说了什么？”

ConversationState：

“客户现在在做什么？”

RequirementProfile：

“目前我们知道什么？”

SpeechAct：

“客户这句话是什么类型？”

Dialogue Policy：

“现在应该做什么？”

Recommendation Engine：

“推荐什么？”

Engineering Calculator：

“怎么算？”

ResponseContext：

“允许 LLM 使用哪些事实？”

ResponseGenerator：

“怎么自然地说？”

ResponseValidator：

“说得是否合法？”

FinalResponseGuard：

“最终是否只能输出一个合规回复？”

==================================================
三十一、最终验收标准　✅ 12 条逐条满足（对应测试见实施记录）

必须全部满足：

[1]

任何一个 Customer Turn：

最多一个客户可见 Response。

[2]

任何一个 Response：

最多一个 Question。

[3]

任何一个 Dialogue Turn：

最多一个 Selected DialogueAction。

[4]

客户回答上一问题：

系统能够识别：

answer_to = last_question_slot。

[5]

客户答非所问：

信息不能丢失。

[6]

已经回答的字段：

不能重复询问。

[7]

environment 未确定：

优先询问 indoor/outdoor。

[8]

明显场景：

继续允许现有 indoor/outdoor 推断。

[9]

P3：

继续支持 pitch → viewing distance 的现有推导。

[10]

产品不存在精确 P3：

使用：

requested_pitch

resolved_pitch

pitch_match_type

pitch_resolution_reason

进行透明解释。

[11]

LLM：

不能新增 Context 之外的业务事实。

[12]

当前阶段：

不增加 Long-term Memory。

==================================================
三十二、最终目标　✅ 已达成

最终系统不应该是：

Customer
→ LLM
→ 再问一个问题
→ LLM
→ 再问一个问题

而应该是：

Customer

↓

Requirement Extraction

↓

Conversation State

↓

Requirement State

↓

SpeechAct

↓

Dialogue Policy

↓

ONE Action

↓

Engineering / Recommendation

↓

ResponseContext

↓

LLM

↓

ONE Response

这样你现在的两个核心问题：

“为什么一次出现两个问题？”

以及：

“为什么 AI 不知道客户是在回答上一轮哪个问题？”

会从架构层得到解决，而不是继续依赖 Prompt 打补丁。

本阶段最重要的两个改造点只有：

第一：

ONE TURN → ONE ACTION → ONE RESPONSE

第二：

RequirementProfile + ConversationState 双状态架构

长期 Memory 暂时完全不参与。

==================================================
三十三、实施记录（v2.6，2026-09-21）

全量测试：`python -m pytest tests/ -q` → **1597 passed, 4 skipped**
（含 §三十四 的日志复现修补：环境优先级 + 接线 + 日志口径）
（v2.5++++ 基线 1527 passed；本轮新增 62 条对话层测试，0 回归）

──────────────────────────────────────────────────
一、§四 彻底解决“一轮两个问题”

§4.1 梳理所有输出入口（结论）

  排查了 `src/orchestrator.py` / `src/agents/sales/runner.py` /
  `nodes/script_generator.py` / `src/dialogue/*` / `src/api.py` / `static/app.js`
  里所有 response / message(s) / extra_messages / follow_up / question /
  pending_question / next_question / reply 字段，找到三个真实出口：

    ① `result["response"]`（主回复，可能自带 2 个问句）；
    ② `result["extra_messages"]`（多屏追问等追加气泡，前端**另起一条** AI 消息）；
    ③ `first_contact_messages`（首次接待：自我介绍 + 案例视频 + 名片）。

  ②就是"一轮两个问题"在前端表现为两个气泡的根因；③是素材投递，不是对话回复。

§4.2 唯一客户可见出口　✅

  新增 `src/dialogue/final_response.py`：

    FinalResponseCoordinator.build(text, extras, questions, action, …) → FinalResponse

  · 追加气泡**并入同一条文本**（不再作为第二条气泡发送）；
  · 合并后统一过 `FinalResponseGuard` → 全局最多一个问题；
  · `orchestrator._finalize_turn_response()` 改为调用它，
    并 `result.pop("extra_messages")`；API 层 `extra_messages` 恒为空列表。

§4.3 统一最终返回对象　✅

  `FinalResponse`：`text / action / question_count / question_slot /
  facts_used / warnings / turn_id / validation_result / response_count`。
  编排结果里放 `result["final_response"]`，API 只取 `answer`（= FinalResponse.text）。

§4.4 / §4.5 唯一 Action　✅

  `src/dialogue/action.py` 新增 `ONE_TURN_ONE_ACTION` 与
  `select_single_action(candidates) → (selected, discarded)`；
  编排层 `_select_turn_action()` 把"Policy 选的动作 + QuestionFlow 想追问的项"
  收成一个（按优先级，并列保留先出现的），
  其余写进 `result["discarded_actions"]` / `action_candidates`。

§4.6 answer_then_ask 最多一个问题　✅

  `FinalResponseGuard` + `FinalResponseCoordinator` 双重约束：
  `question_count <= 1`；测试 `tests/dialogue/test_one_question_per_turn.py`。

§4.7 FinalResponseGuard 成为最后防线　✅

  收口链固定为：售后口径 → 图片核对 → FinalResponseGuard → FinalResponse；
  仍超预算时 `_hard_repair()` 只留第一句问句，并把
  `validation_result` 标为 REPAIRED / FAIL 留痕。

§4.8 Validator 不重新决定业务　✅

  `response_validator` 仍只做安全 / 事实 / 格式 / 问题数量检查；
  业务决定只在 Dialogue Policy + QuestionFlow（本轮未让 Validator 选问题）。

──────────────────────────────────────────────────
二、§五~§十 ConversationState（双状态架构）

文件：`src/dialogue/conversation_state.py`（在 v2.3 模块上扩展，不是新建重复模块）

§6 字段全量落地：

  last_ai_action / last_ai_question / last_question_slot /
  current_speech_act / current_answer_slot / pending_customer_question /
  pending_customer_answer / last_turn_id / current_turn_id / last_response
  （旧的 last_action / last_slot / last_question / asked_slots 全部保留兼容）

§7 与 RequirementProfile 严格分开：档案管"客户有什么需求"，
  对话状态管"客户现在在做什么"；测试 `test_conversation_state.py` 有专门用例。

§8 每次 AI 问问题都记录：

  `ConversationState.note_ai_turn(action, question, slot, response, turn_id, speech_act)`
  由 `orchestrator._note_ai_turn()` 每轮调用一次（含 turn_id）。

§9 回答优先匹配上一问题：新增纯函数

  `match_answer_to_question(message, last_question_slot=…)`
  → `ANSWER_PREVIOUS_QUESTION / ANSWER_WRONG_SLOT / NEW_REQUIREMENT / NO_REQUIREMENT`

  · "都行 / both" 这类没有信息量的回答按"上一轮问的是哪一项"落地；
  · 返回 `slots`（抽取器原始槽位，信息不丢）+ `covered_slots`（对话层槽位名）。

§10 答非所问不丢信息：AI 问 indoor/outdoor、客户答 3×5 →
  `ANSWER_WRONG_SLOT(slot=size)`；尺寸记 ANSWERED，环境保持未知、下一轮照样问。

§11 一次多个信息：`explicit_slots()` / `canonical_slots()` 把
  "Indoor, 3×5m, P3, fixed" 一次性落成 4 个字段，只更新一次、只问一个问题。

编排接线：`orchestrator._note_customer_turn()` 在每轮开头记账
（客户说完一句先判"在答哪一项"），`_note_ai_turn()` 在收口后记账（含 turn_id）。

──────────────────────────────────────────────────
三、§十一~§十五 现有业务口径全部保留

§12 环境第一优先级：`environment_gate_plan()` 仍是"随机轮之前的第一问"；
  明显场景（church / meeting room / outdoor billboard…）继续允许推断；
  测试 `test_environment_first.py`（含 Case 1 / Case 2）。

§13 随机机制：随机仍只在"同等业务价值"之间发生（`rank_slots_by_value`），
  environment 未定时不会被随机跳过；测试 `test_environment_first.py::test_pitch_alone…`。

§14 P3 ↔ 视距：推导继续走结构化窗口（`src/engineering/pitch_window.py`、
  `src/rag/pitch_resolution.py`），不由 LLM 猜；测试 `test_pitch_resolution_context.py`。

§15 P3 无精确型号：`requested_pitch / resolved_pitch / pitch_match_type /
  pitch_resolution_reason` 继续透明解释（`PitchResolution.explain()`）。

──────────────────────────────────────────────────
四、§十六~§二十 决策与表达层

§16/§17 Dialogue Policy：

  `src/dialogue/policy.py` 的优先级（P0 客户问题 > P1 纠正/冲突 > P2 新需求 >
  P3 Gate 必问 > P4 最有价值下一问 > P5 补充）保持不变，
  本轮补上"候选 → 唯一 Action"的收口（见 §4.4），并把 SpeechAct 结果透传进日志；
  测试 `test_customer_question_priority.py`（Case 7：How long is delivery? → 只回答）。

§18 ResponseContext：已具备 `grounded_facts` / `pitch_resolution` / `one_question`
  等字段（v2.5+++），本轮把本轮事实顺带写进 `FinalResponse.facts_used` 便于审计。

§19 LLM 事实规则：`grounded_facts.ALLOWED_SOURCES` 白名单未放宽，未新增事实来源。

§20 ResponseGenerator：仍是"只负责把 Action + Context 说自然"，
  本轮没有给它任何新的业务判断（不选问题、不选产品、不算工程）。

──────────────────────────────────────────────────
五、§二十一~§二十二 重复提问与问题状态机

新增 `src/dialogue/question_registry.py`：

  QuestionRecord(slot, asked_at_turn, asked_count, answered, answer_turn, state)
  AskedQuestionRegistry.note_asked / note_answered / mark_state /
  has_asked / is_answered / should_skip / to_dict

  状态机：UNKNOWN → ASKED → ANSWERED → CONFIRMED / INFERRED / RESOLVED
  （SETTLED_STATES 不再问）

接线：`ConversationState.registry` + `question_flow.next_question_plan()`
  把登记簿里"已有结论"的槽位并入 `skip` ——
  即使档案被重建，也不会重复问同一项；
  测试 `test_no_duplicate_question.py`（含 Case 5）。

──────────────────────────────────────────────────
六、§二十三~§二十四 多消息与 API

§23 Message Aggregator 一行未改：聚合后仍然"一次决策"；
  本轮保证决策结果**只有一条**客户可见回复（追加气泡并入正文）。

§24 API 收口（`src/api.py`）：

  ChatResponse 新增 `turn_id / action / question_slot / question_count /
  response_count / debug_context`；
  `answer` 即唯一回复；`extra_messages` 恒为空；
  `debug_context` 只放内部信息（会话状态 / 决策审计 / FinalResponse），
  前端 `static/app.js` 只在**老后端**（无 `response_count`）时才渲染额外气泡。

──────────────────────────────────────────────────
七、§二十五~§二十六 测试体系

`tests/dialogue/` 共 12 个测试文件（计划点名的 10 个 + 回归总表 + 端到端）：

  test_one_question_per_turn.py          §4.5/4.6/4.7 + Case 10
  test_conversation_state.py             §5~§8
  test_answer_to_previous_question.py    §9
  test_answer_wrong_slot.py              §10 + Case 8
  test_environment_first.py              §12/§13 + Case 1/2
  test_multi_requirement_update.py       §11 + Case 6
  test_customer_question_priority.py     §17 + Case 7
  test_no_duplicate_question.py          §21/§22 + Case 5
  test_final_response_single_output.py   §4.3/§4.7/§24 + Case 9
  test_pitch_resolution_context.py       §14/§15
  test_v26_regression_cases.py           §26 Case 1~10
  test_orchestrator_single_output.py     端到端：编排器只产出一条可见回复

结果：`python -m pytest tests/dialogue -q` → **62 passed**

──────────────────────────────────────────────────
八、§二十七~§二十八 可观测性与决策审计

§27 `[Turn]` 一行日志（`src/orchestrator.py::_finish_llm_turn`）现在包含：

  session / messages / aggregated / llm_calls / llm_latency_ms / llm_tokens /
  total_ms / **turn_id / action / speech_act / question_slot /
  response_count / question_count / validation**

  `result["_turn"]` 同步带上这些字段（含 llm_calls / llm_latency_ms /
  total_latency_ms），`PerfTracker` 也加了同名字段。

§28 `[DecisionAudit]` 每轮一条：turn_id / input / speech_act /
  conversation_state_before / candidate_actions / selected_action /
  discarded_actions / final_response / question_count / question_slot /
  validation_result（`orchestrator._emit_decision_audit`）。
  → 以后回答"为什么这一轮问了这个问题"直接查 turn_id，而不是"LLM 自己决定的"。

──────────────────────────────────────────────────
九、§二十九 守住的红线

  · 没有增加长期 Memory / 向量 Memory（ConversationState 仍是进程内、会话级）；
  · 没有重做 Customer Profile、RAG、Recommendation Engine；
  · 没有改 Vision / Message Aggregator / SpeechAct 的既有判定；
  · 没有用新的 Prompt 规则去补业务逻辑；
  · 没有删除"明显场景 → indoor/outdoor 推断"（原逻辑完整保留）；
  · 没有删除 pitch ↔ viewing distance 推导（原逻辑完整保留）；
  · ResponseValidator / ResponseGenerator 没有获得业务决策权。

──────────────────────────────────────────────────
十、§三十一 验收标准逐条对应

  [1] 一个 turn 最多一条客户可见回复 → `FinalResponse.response_count == 1`
      + `test_orchestrator_single_output.py`
  [2] 一条回复最多一个问题 → `FinalResponseGuard` + `test_one_question_per_turn.py`
  [3] 一个 turn 最多一个 Selected Action → `select_single_action` + Case 10
  [4] 识别 answer_to = last_question_slot → `test_answer_to_previous_question.py`
  [5] 答非所问不丢信息 → `test_answer_wrong_slot.py`
  [6] 已答字段不重复问 → `test_no_duplicate_question.py`
  [7] environment 未定优先问 → `test_environment_first.py`
  [8] 明显场景推断保留 → `TestEnvironmentFirst::test_obvious_scenario_keeps_inferring`
  [9] P3 → 视距推导保留 → `test_pitch_resolution_context.py`
  [10] requested/resolved/匹配类型/原因 → 同上（`PitchResolution.to_dict()`）
  [11] LLM 不能加 Context 之外的事实 → `grounded_facts` 白名单（本轮核对）
  [12] 不增加长期 Memory → 本轮无新增持久化（ConversationState 仍为进程内）

──────────────────────────────────────────────────
十一、已知边界（本轮没做、留在下一阶段）

  · First Contact 的素材通道（自我介绍 + 视频 + 名片）仍按固定流程多条投递 ——
    计划 §4 的"唯一回复"约束只作用于**对话回复**，没有动素材投递；
  · `ConversationState` 仍是进程内状态（服务重启即清空），
    持久化属于计划里明确排除的长期 Memory 范畴；
  · DecisionAudit 目前写日志；若以后要做前端"为什么这么问"面板，可直接读
    `debug_context.decision_audit`。

==================================================
三十四、修订记录（真实日志复现，2026-09-21 14:50）

现象（客户提供的真实日志 session_1789973381900_rfwgwgsln）：

  ① `[Turn] ... action=ask speech_act=- question_slot=-` —— §27 要求的观测字段全是空；
  ② `[DecisionAudit] "speech_act": ""`、`"question_slot": ""`、`selected_action: "ask"`
     （Policy 实际给的是 `ask_only`）；
  ③ 客户被问"indoor or outdoor"后答 "3*5" → 系统改问 "Do you already know the pitch…"，
     室内外一直没被确认（计划 §26 Case 2 期望：这一轮的问题仍然是 indoor / outdoor）；
  ④ `PERF ... llm_calls=0` 与同一轮 `[Turn] ... llm_calls=2` 自相矛盾。

根因（逐条定位，均已修复）：

  ① `src/agents/sales/state.py` 的 `SalesState` 没声明 `speech_act` / `dialogue_action`
     → LangGraph 不把这两个键带出图，上层永远拿到空值。
     ✅ 已补进 state schema（并加 `response_plan`）。

  ② `orchestrator` 的结果字典在 "ask / greeting" 分支里**没有透传**
     `pending_question / pending_slot`（只有 solution 分支用过），
     于是收口层不知道"这一轮问的是哪一项"。
     ✅ 已在分支统一 `setdefault` 透传（question_slot 现在能正确落到日志 / API）。

  ③ v2.4 的"答非所问 → 本轮不再重复问同一项"（`_environment_was_just_asked`）
     把环境让位给了随机轮，直接违反计划 §13"环境未定 → 唯一最高优先级"。
     ✅ `environment_should_ask_now()` 改为 **环境未定就问**（不再让位）；
        问满 `MAX_ASKS_PER_SLOT=2` 次后 `is_exhausted` → 交给 Gate 的
        DEFERRED / BLOCKED，不会无限追问；复问统一用**直问**
        （不用 "That's okay — most installations are indoors…" 那种降门槛说法，
        客户曾明确反馈该说法紧接着再出现观感更差）。

     ⚠️ 口径变更说明：这一条**取代** v2.4 的"答非所问先换下一问"。
        相关旧测试已按新口径更新：

          tests/test_v24_question_flow.py::test_answered_once_keeps_environment_as_the_first_question
          tests/test_dialogue_output_chain.py::test_environment_is_still_first_when_customer_answers_something_else
          tests/test_dialogue_output_chain.py::test_environment_returns_in_hard_recap_with_plain_wording

  ④ `PERF` 那行在 `end_turn()` 之前打，只能看到手工计数器。
     ✅ 新增 `PerfTracker.live_llm_calls()`，日志口径与 `[Turn]` 一致。

  附带：`[DecisionAudit]` 现在同时输出"决定之前 / 之后"两份对话状态，
  并补上 `requirement_state_before`（拿不到就留空，不影响业务）。

新增测试：

  tests/dialogue/test_turn_wiring.py      —— ① ② ④ 的接线回归（5 条）
  tests/dialogue/test_environment_first.py::TestEnvironmentAfterWrongAnswer
                                          —— ③ 的回归（3 条：仍是第一问 / 用直问 / 两次上限）

验证：`python -m pytest tests/dialogue -q` → **70 passed**
      `python -m pytest tests/ -q` → **1597 passed, 4 skipped**

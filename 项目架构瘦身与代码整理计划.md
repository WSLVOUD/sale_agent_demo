# iSEMC LED / LCD / IFP AI 销售系统架构瘦身与代码整理计划

> 目标：**不改变现有核心功能、不改变现有业务规则、不改变推荐结果，只整理代码结构、消除重复职责、降低维护复杂度。**
>
> 原则：**先兼容、后迁移；先验证、后删除；不做“大重构式重写”。**

---

## 执行进度（2026-09-24；本次执行不提交）

| 阶段 | 内容 | 状态 | 落地 / 证据 |
|---|---|---|---|
| Phase 0 | 建立项目基线 | ✅ 完成 | `docs/refactor/`：`baseline.md`（Python 3.11.9 / 726 用例 / 12 接口 / 评测基线）、`dependency_map.md`（真实 import 索引）、`module_inventory.md`（160 模块分类）、`behavior_baseline.md`（S1~S7 行为 + 锚点测试） |
| Phase 1 | 统一 RequirementProfile | ✅ 完成（已收敛，补护栏） | 核对：全项目只有 `src/models/requirement.py` 定义 `RequirementProfile`；生产者（extractor / query_understanding / vision）与决策链（Gate / 推荐 / 校验）都只认它；`RequirementBook` 只是多产品登记簿（决策链 0 引用）。新增 `tests/test_architecture_requirement_model.py`（6 条） |
| Phase 2 | 统一 Product Type Router | ✅ 完成（真正瘦身） | 决策函数 `route_display_type` 全项目唯一；**产品类型词汇改为单一来源**：`product_type_router` 不再自己写 LED/LCD/UNKNOWN，改为引用 `turn_kind`（值不变，行为不变）。新增 `tests/test_architecture_product_type.py`（6 条） |
| Phase 3 | 统一 Turn / Question Decision | ✅ 完成（核对 + 消除重复规则） | 核对：`action / turn_action / question_order / question_registry / question_planner / question_flow` 是**流水线不是重复**（每级职责唯一），按计划原则不做无收益合并；**"一轮最多一个问题"的预算原来写 4 份**，现统一为 `action.MAX_QUESTIONS_PER_TURN` 单一来源。新增 `tests/test_architecture_turn_decision.py`（8 条） |
| Phase 4 | 整理 Response 层 | ✅ 完成（边界契约化；合层暂缓） | 已核对并钉住"客户可见文本只有一个出口"（`orchestrator → FinalResponseCoordinator.build`）。多层合并（Context/Planner/Generator/Natural/Continuation→三段）**暂缓**：这条链上挂着客户口径（承接额度 / 单一问题 / 自然措辞），没有逐条行为证据前不动。见 `tests/test_architecture_boundaries.py` |
| Phase 5 | Sales / Solution 去重复 | ✅ 完成 | 核对：Solution 侧 `understand_node` 复用同一个 `RequirementExtractor` → `RequirementProfile`，旧字段只是投影；旧的自然语言抽取降级为"没有档案时的兜底"。新增 `tests/test_architecture_agents_and_recommendation.py`（7 条） |
| Phase 6 | 整理 Recommendation | ✅ 完成 | 核对并钉住单一入口：业务侧只允许 `RecommendationService`（Gate 必过）→ coordinator（编排/可行性/审计）→ engine（评分排序）；引擎与 coordinator 都不许被绕过。同上测试文件 |
| Phase 7 | 整理 RAG 边界 | ✅ 完成 | 包级 import 矩阵实测：**RAG 不引用 dialogue / agents**（只做知识，不做对话决策）；非 Gate 模块不得自行判"推荐就绪"。见 `tests/test_architecture_boundaries.py` |
| Phase 8 | 整理 Calculation / Vision / Session | ✅ 完成 | 计算层（engineering + screen_calculator）不引用 agents/dialogue；Vision 不出现推荐/问题计划/决策符号；会话层只依赖 config。同上测试文件 |
| Phase 9 | 测试重组 | ✅ 完成（分类 + 方案；搬迁暂缓） | `docs/refactor/test_inventory.md`：86 个文件按 9 个功能域分类 + Regression 清单。物理搬迁暂缓：大量用例靠 `__file__` 深度推算项目根，先统一路径注入再搬（否则就是"改目录再修报错"，计划 §2 禁止） |
| Phase 10 | 删除确认后的 Legacy | ✅ 完成 | 删除 `src/core/sales_requirement_adapter.py`、`src/core/solution_requirement_adapter.py`（引用索引：src 0 + tests 0；只做 RequirementExtractor 包装与旧字段投影，无独有业务逻辑）。src 文件数 161 → 159 |
| Phase 11 | README / Docs 清理 | ✅ 完成 | README 顶部版本/测试数更新为 v2.9.6 / 760 条；新增「架构整理（2026-09-24）」一节（指向 `docs/refactor/`、本轮改动、验收数字）；历史章节数字标注为当时快照 |
| 收尾 | 瘦身后全量测试 | ✅ 完成 | `python -m pytest -q` → **760 passed in 158.45s**；评测与基线逐项一致（计算 1.0 / 槽位 0.7032 / 路由 0.7439 / 检索 MRR 0.6708 / 违规率 0.0） |

### 本轮改动文件

```text
新增  docs/refactor/baseline.md            基线与验收纪律
新增  docs/refactor/dependency_map.md      依赖地图（真实 import 索引）
新增  docs/refactor/module_inventory.md    模块分类（含 LEGACY 判定方法）
新增  docs/refactor/behavior_baseline.md   行为基线（S1~S7 + 锚点测试）
新增  docs/refactor/test_inventory.md      测试分类与迁移方案
新增  tests/test_architecture_requirement_model.py     （Phase 1 护栏，6 条）
新增  tests/test_architecture_product_type.py          （Phase 2 护栏，6 条）
新增  tests/test_architecture_turn_decision.py         （Phase 3 护栏，8 条）
新增  tests/test_architecture_boundaries.py            （Phase 4/7/8 护栏，7 条）
新增  tests/test_architecture_agents_and_recommendation.py （Phase 5/6 护栏，7 条）
改动  src/dialogue/product_type_router.py   词汇改为引用 turn_kind（单一来源）
改动  src/dialogue/final_guard.py           预算改为引用 action.MAX_QUESTIONS_PER_TURN
改动  src/dialogue/final_response.py        同上
改动  src/dialogue/response_context.py      同上
删除  src/core/sales_requirement_adapter.py     （零引用旧适配层）
删除  src/core/solution_requirement_adapter.py  （零引用旧适配层）
改动  README.md                             版本/测试数 + 「架构整理」一节
```

### 明确暂缓的动作（附原因，不是遗漏）

```text
1. Response 层合层（Context/Planner/Generator/Natural/Continuation → 三段）
   原因：链上挂着客户口径行为（一轮一问、承接额度、自然措辞），
        需要先给这些行为各补一条"合层后仍然成立"的证据。
2. 测试目录物理搬迁
   原因：路径样板按 __file__ 深度推算，先统一注入方式再搬。
3. Phase 1 里点名的两个适配层已按计划删除；models→rag 与 dialogue→agents
   的 3 + 1 处**函数内延迟导入**保留（已知例外，已在护栏里钉死数量，
   不再扩散；彻底搬迁需要先把 question planner / 目的推断下沉到共享层）。
```

---

## 1. 本次改造范围

### 1.1 必须保留的核心功能

本次整理不得破坏以下现有能力：

- LED / LCD / IFP 产品推荐
- LED 室内 / 室外判断
- LCD / IFP 现有需求链入口与推荐逻辑
- Recommendation Ready Gate
- Calculation Ready Gate
- 产品硬过滤
- 产品评分与排序
- RAG 混合检索
- BGE-M3 向量检索
- BM25 / Sparse / RRF
- 产品结构化 JSON 数据
- TXT / 产品知识库
- Screen Calculator
- 尺寸 / 面积 / 像素间距 / 分辨率等计算
- Sales Agent
- Solution Agent
- First Contact
- Session / Turn 级上下文
- 多条客户消息合并处理
- 防止同一轮连续追问多个问题
- Response Validator / Final Guard
- 图片需求识别
- FastAPI 接口
- 现有前端聊天功能
- 现有 WhatsApp 对接入口
- 当前 API / 外部调用方式

### 1.2 本次明确不做

以下内容暂时不进行功能升级：

- 不重新设计 LED 推荐算法
- 不重新设计 LCD 推荐算法
- 不重新设计 IFP 推荐算法
- 不重新设计推荐评分公式
- 不修改现有产品数据
- 不更换 LLM
- 不更换 Embedding 模型
- 不更换 Chroma
- 不更换 LangGraph
- 不重新设计 RAG
- 不增加长期记忆
- 不重写前端
- 不改变 WhatsApp 工作流
- 不把 LangGraph 改成 n8n
- 不增加新的销售策略
- 不重新设计 First Contact
- 不重新设计图片识别功能

---

# 2. 总体改造原则

## 原则一：功能冻结

开始整理前先建立当前版本基线。

记录：

- 当前测试数量
- 当前测试通过率
- 当前启动方式
- 当前 API
- 当前推荐结果
- 当前关键对话流程
- 当前 RAG 检索结果
- 当前计算结果

后续每完成一个模块迁移，都必须与基线对比。

---

## 原则二：不直接删除旧代码

禁止：

```text
发现文件重复
↓
直接删除
↓
再修报错
```

必须：

```text
分析调用关系
↓
确定真正职责
↓
建立新入口
↓
迁移调用方
↓
测试
↓
确认没有引用
↓
最后删除
```

---

## 原则三：不一次性重构整个项目

禁止一次性：

- 改目录
- 改类名
- 改函数名
- 改状态结构
- 改 Agent
- 改推荐逻辑

全部同时进行。

采用：

```text
一个职责
→ 一个统一入口
→ 迁移调用
→ 测试
→ 删除重复代码
```

---

# 3. 第一阶段：建立项目基线

## 目标

在任何代码移动之前，先确认当前系统是什么状态。

## 工作内容

建立：

```text
docs/refactor/
├── baseline.md
├── dependency_map.md
├── module_inventory.md
└── behavior_baseline.md
```

### baseline.md

记录：

- Python 版本
- 依赖版本
- 启动方式
- API 入口
- 测试数量
- 测试通过率

### dependency_map.md

记录：

```text
API
 ↓
Turn / Session
 ↓
Sales / Solution
 ↓
Requirement
 ↓
Product Type
 ↓
RAG / Recommendation
 ↓
Response
```

同时记录真实 import 关系。

### module_inventory.md

给所有 Python 文件分类：

```text
CORE
DUPLICATE
ADAPTER
COMPATIBILITY
LEGACY
TEST_ONLY
```

### behavior_baseline.md

建立关键行为样例：

```text
用户：i need a display
用户：indoor
用户：3m x 5m
用户：viewing distance 8m
```

以及：

```text
LED
LCD
IFP
室内
室外
推荐
计算
RAG
图片
多消息
```

全部保存预期结果。

---

# 4. 第二阶段：建立唯一的 Requirement 数据模型

## 目标

解决项目中多个地方分别保存 / 转换客户需求的问题。

当前项目中存在：

```text
requirement_extractor
sales_requirement_adapter
solution_requirement_adapter
sales/nodes/requirement
solution/nodes/requirement
query_understanding
parameter_inference
environment resolver
```

这些模块不能继续各自维护一套需求定义。

---

## 新原则

整个系统只允许存在一个核心：

```text
RequirementProfile
```

所有模块都围绕它工作。

例如：

```python
RequirementProfile(
    product_type=None,
    environment=None,
    installation=None,
    width=None,
    height=None,
    viewing_distance=None,
    pixel_pitch=None,
    brightness=None,
    resolution=None,
    handwriting=False,
    waterproof=False,
    splicing=False,
    constraints=[],
    preferences=[],
    assumptions=[],
)
```

具体字段以当前项目实际模型为准。

---

## 数据流

统一为：

```text
客户消息
   ↓
Requirement Extractor
   ↓
RequirementProfile
   ↓
Turn Engine
   ↓
Product Type / Recommendation / Calculation
```

禁止：

```text
Sales 一套 Requirement
Solution 一套 Requirement
RAG 再自己解析一次
Recommendation 再自己解析一次
```

---

## 迁移方式

先保留：

```text
sales_requirement_adapter.py
solution_requirement_adapter.py
```

但让它们只做：

```text
旧结构
 ↓
RequirementProfile
```

暂时作为兼容层。

等所有调用方迁移完成后再删除。

---

# 5. 第三阶段：整理 Product Type 判断

## 目标

只建立一个产品类型判断入口。

统一处理：

```text
LED
LCD
IFP
UNKNOWN
```

---

## 当前功能不改变

保留现有业务边界：

```text
客户明确 LED
    ↓
LED

客户明确 LCD
    ↓
LCD

客户明确 IFP
    ↓
IFP

客户说需要手写
    ↓
LCD / IFP 路径

室外显示
    ↓
保留现有 LED 逻辑
```

具体现有规则不在本阶段修改。

---

## 整理对象

重点检查：

```text
product_router.py
product_type_router.py
product_type_understanding.py
```

最终只保留一个真正负责：

```text
ProductTypeRouter
```

其他文件：

- 如果只是包装 → 合并
- 如果只是转发 → 删除
- 如果是兼容接口 → 暂时保留
- 如果有独立有效逻辑 → 提取后归入统一 Router

---

## 重要限制

本阶段只做：

```text
代码职责统一
```

不做：

```text
LED/LCD/IFP 规则重新设计
```

这样可以保证不会影响现有 LED 推荐。

---

# 6. 第四阶段：整理 Dialogue 层

这是当前项目最需要瘦身的部分之一。

目前存在大量类似职责：

```text
action.py
turn_action.py
question_planner.py
question_flow.py
question_order.py
question_registry.py
response_context.py
response_coordinator.py
response_planner.py
response_generator.py
natural_response.py
natural_continuation.py
momentum.py
continuation_budget.py
final_guard.py
response_validator.py
final_response.py
```

目标不是全部删除。

目标是：

> 一个职责只保留一个真正的决策源。

---

# 7. Turn Decision 统一

最终建立一个核心：

```text
TurnEngine
```

负责：

```text
输入客户消息
    ↓
合并本轮消息
    ↓
提取新增需求
    ↓
更新 RequirementProfile
    ↓
判断当前状态
    ↓
决定下一步动作
    ↓
生成响应
```

---

## TurnAction 统一

将：

```text
action.py
turn_action.py
```

进行职责检查。

最终只允许一个：

```python
TurnAction
```

例如：

```text
ACK
ASK_REQUIREMENT
CLARIFY
RECOMMEND
CALCULATE
ANSWER
RETRIEVE
HANDOFF
```

实际枚举以当前项目为准。

---

# 8. Question Planner 合并

以下模块重点进行合并：

```text
question_planner.py
question_flow.py
question_order.py
question_registry.py
```

最终形成一个统一入口，例如：

```text
QuestionPolicy
```

负责：

```text
当前已经知道什么？
↓
当前缺什么？
↓
什么是下一步最有价值的信息？
↓
一次只问一个问题
```

---

## 必须保留的现有行为

### 规则 1

一轮只允许一个主要问题。

### 规则 2

如果客户一次发送多个消息：

```text
消息 A
消息 B
消息 C
```

先合并：

```text
A + B + C
```

再进行一次决策。

### 规则 3

客户已经提供的信息不得重复询问。

### 规则 4

如果客户回答的是另一个需求，也必须先吸收。

例如：

```text
AI：Where will the display be installed?

客户：3m × 5m.
```

不能因为客户没有回答环境就直接忽略尺寸。

必须：

```text
更新 size
↓
继续判断 environment
```

---

# 9. Response 层整理

重点整理：

```text
response_context.py
response_coordinator.py
response_planner.py
response_generator.py
natural_response.py
natural_continuation.py
```

目标：

```text
ResponseContext
      ↓
ResponsePlanner
      ↓
ResponseGenerator
```

而不是：

```text
Planner
 ↓
Coordinator
 ↓
Natural
 ↓
Continuation
 ↓
Generator
 ↓
Final
```

---

## 保留自然语言能力

本次不是把 AI 回复改成死板模板。

仍然保留：

- acknowledgement
- natural continuation
- conversational tone
- customer context
- requirement-aware response

只是减少中间层。

---

# 10. Final Guard / Validator 整理

重点：

```text
final_guard.py
response_validator.py
final_response.py
```

最终保留一个明确的最终校验入口。

例如：

```text
ResponseValidator
```

负责：

```text
是否重复问题
是否连续问两个问题
是否出现未确认产品参数
是否与 RequirementProfile 冲突
是否出现明显幻觉
是否违反当前状态
是否遗漏客户刚刚提供的信息
```

---

# 11. Sales Agent / Solution Agent 整理

## 不删除两个 Agent

现阶段仍然保留：

```text
Sales Agent
Solution Agent
```

因为它们承担不同任务。

但是：

> 两个 Agent 不应该各自维护完整的 Requirement 系统。

---

## 改造方向

变成：

```text
                    ┌─ Sales Task
TurnEngine ──────────┤
                    └─ Solution Task
                         ↓
                 Shared RequirementProfile
```

Sales Agent 负责：

```text
客户沟通
需求推进
问题询问
销售话术
```

Solution Agent 负责：

```text
技术理解
RAG
推荐
计算
方案
```

---

## 删除重复

重点检查：

```text
agents/sales/nodes/requirement.py
agents/solution/nodes/requirement.py
```

如果只是重复提取需求：

```text
→ 统一调用 RequirementExtractor
```

如果存在特殊业务逻辑：

```text
→ 保留特殊逻辑
```

绝对不直接删除。

---

# 12. Recommendation 层整理

现有核心功能全部保留。

重点整理：

```text
readiness.py
recommendation_engine.py
recommendation_coordinator.py
recommendation_service.py
validation.py
model_guard
hard_filter.py
```

最终结构：

```text
RecommendationGate
        ↓
HardFilter
        ↓
RecommendationEngine
        ↓
RecommendationValidator
```

---

## 各模块职责

### RecommendationGate

判断：

```text
现在是否允许推荐？
```

不负责推荐哪个产品。

### HardFilter

处理：

```text
硬性条件
```

### RecommendationEngine

处理：

```text
候选产品
↓
评分
↓
排序
```

### RecommendationValidator

检查：

```text
推荐结果
↓
是否满足客户条件
↓
是否存在参数幻觉
```

---

## 禁止

不要把：

```text
Recommendation
Question
Response
RAG
```

混在一个函数里。

---

# 13. RAG 层整理

RAG 只负责：

```text
理解查询
↓
检索
↓
返回知识
```

不要让 RAG 决定：

```text
客户现在该不该推荐
客户下一步该问什么
最终推荐哪个产品
销售话术
```

---

## 重点检查

```text
query_understanding.py
corpus.py
router.py
retriever
vectorstore
BM25
RRF
```

目标：

```text
RAG = Knowledge Retrieval
```

而：

```text
Recommendation = Business Decision
```

两者边界明确。

---

# 14. Query Understanding 整理

`query_understanding.py` 目前如果同时承担：

```text
Query Parsing
Requirement Extraction
Recommendation Decision
Readiness
Response
```

必须拆职责。

最终：

```text
Query Understanding
        ↓
结构化 Query
        ↓
RequirementProfile
```

不允许 Query Understanding 直接决定最终产品。

---

# 15. Calculation 层保持独立

保留：

```text
src/tools/screen_calculator.py
```

以及现有计算逻辑。

计算流程：

```text
RequirementProfile
        ↓
Calculation Ready Gate
        ↓
Screen Calculator
        ↓
Calculation Result
        ↓
Validation
```

不要把计算代码塞进：

```text
Sales Agent
Solution Agent
Recommendation Engine
```

---

# 16. Vision 层保持独立

图片识别继续保持：

```text
Image
 ↓
Vision Model
 ↓
Requirement Extraction
 ↓
RequirementProfile
```

不要让 Vision 模块自己：

```text
推荐产品
决定销售问题
生成最终答案
```

Vision 只负责：

```text
从图片中提取可见需求信息
```

---

# 17. Memory / Session 保持轻量

当前重点是：

```text
Session
Turn
Conversation Context
```

不扩展长期记忆。

建议：

```text
ConversationState
    ├── RequirementProfile
    ├── current_turn
    ├── recent_messages
    ├── pending_action
    └── conversation_flags
```

避免多个模块分别维护：

```text
自己的客户状态
自己的需求状态
自己的历史消息
```

---

# 18. 文件整理方式

不要直接大规模移动。

采用：

```text
第一步：新建统一入口
第二步：旧文件调用新入口
第三步：迁移业务调用
第四步：运行测试
第五步：删除旧实现
```

---

# 19. 推荐的目标目录

最终可以逐步收敛到：

```text
src/
├── api.py
├── config.py
│
├── application/
│   └── turn_engine.py
│
├── conversation/
│   ├── state.py
│   ├── requirement.py
│   ├── turn.py
│   └── response.py
│
├── product/
│   ├── type_router.py
│   ├── catalog.py
│   └── profile.py
│
├── recommendation/
│   ├── gate.py
│   ├── engine.py
│   └── validator.py
│
├── rag/
│   ├── query.py
│   ├── retriever.py
│   ├── corpus.py
│   └── store.py
│
├── engineering/
│   ├── calculator.py
│   ├── resolution.py
│   └── constraints.py
│
├── vision/
│   ├── pipeline.py
│   └── extractor.py
│
├── memory/
│   └── session.py
│
├── agents/
│   ├── sales/
│   └── solution/
│
└── tools/
    └── screen_calculator.py
```

注意：

> 这是最终收敛方向，不要求一次性改成这个目录。

---

# 20. 测试整理

当前测试如果存在大量：

```text
test_v24_xxx
test_v26_xxx
test_v27_xxx
test_v28_xxx
test_v291_xxx
test_v292_xxx
test_v293_xxx
test_v294_xxx
```

不要立即删除。

先按照功能重新分类：

```text
tests/
├── conversation/
├── requirement/
├── product/
├── recommendation/
├── rag/
├── calculation/
├── vision/
├── integration/
└── regression/
```

---

## 历史测试处理

每个旧测试判断：

```text
仍然验证当前功能
    ↓
保留并重新命名

验证已经删除的旧行为
    ↓
删除

重复测试
    ↓
合并

只验证历史 Bug
    ↓
转换成 Regression Test
```

---

# 21. 必须建立的 Regression Tests

至少固定以下场景。

## 场景 1：简单需求

```text
i need a display
```

不能一次问多个问题。

---

## 场景 2：客户一次给多个信息

```text
I need a display.
It's 3m x 5m.
It will be indoor.
Viewing distance is 8m.
```

必须一次提取。

不能：

```text
重复问 size
重复问 environment
重复问 distance
```

---

## 场景 3：回答非当前问题

```text
AI：Where will it be installed?

客户：3m × 5m.
```

必须保存：

```text
size = 3m × 5m
```

然后继续判断 environment。

---

## 场景 4：LED

必须验证：

```text
LED
↓
现有 LED 需求链
↓
现有推荐逻辑
```

---

## 场景 5：LCD

必须验证：

```text
LCD
↓
现有 LCD 需求链
↓
现有推荐逻辑
```

---

## 场景 6：IFP

必须验证：

```text
IFP
↓
现有 IFP 需求链
↓
现有推荐逻辑
```

---

## 场景 7：图片

```text
图片
↓
Vision
↓
RequirementProfile
↓
原有业务链
```

---

## 场景 8：推荐

推荐必须仍然受到：

```text
Recommendation Ready Gate
```

控制。

---

## 场景 9：计算

计算必须仍然受到：

```text
Calculation Ready Gate
```

控制。

---

# 22. 每次重构后的验证方式

每完成一个模块：

```text
单元测试
↓
相关模块测试
↓
Integration Test
↓
Regression Test
```

全部通过后再进入下一模块。

---

# 23. 文件删除标准

一个文件只有同时满足以下条件才允许删除：

```text
1. 没有业务逻辑需要保留
2. 没有外部调用
3. 没有测试依赖
4. 新模块已经接管
5. 全量测试通过
6. Regression Test 通过
7. API 行为没有变化
```

否则：

```text
暂时保留
```

---

# 24. 最终禁止出现的架构问题

整理完成后，不允许出现：

```text
一个需求 → 多套 Requirement 模型
```

```text
一个产品类型 → 多个独立判断源
```

```text
一个问题 → 多个 Question Planner 同时决定
```

```text
一个 Response → 多个 Coordinator 层层包装
```

```text
RAG → 直接决定销售流程
```

```text
Sales Agent → 自己维护一套需求
Solution Agent → 自己维护另一套需求
```

```text
Recommendation → 自己重新解析客户需求
```

---

# 25. 最终目标架构

最终整个系统尽量收敛成：

```text
                     Customer Message
                            │
                            ▼
                    ┌───────────────┐
                    │  Turn Engine  │
                    └───────┬───────┘
                            │
                            ▼
                  ┌──────────────────┐
                  │ RequirementProfile│
                  └────────┬─────────┘
                           │
          ┌────────────────┼────────────────┐
          │                │                │
          ▼                ▼                ▼
   Product Type          RAG          Calculation
      Router           Retrieval         Gate
          │                │                │
          ▼                ▼                ▼
       LED/LCD/IFP      Knowledge       Calculator
          │
          ▼
   Recommendation Gate
          │
          ▼
   Hard Filter
          │
          ▼
 Recommendation Engine
          │
          ▼
 Recommendation Validator
          │
          └──────────────┐
                         ▼
                 Response Generator
                         │
                         ▼
                 Response Validator
                         │
                         ▼
                      Customer
```

---

# 26. 实施顺序

严格按照下面顺序执行：

```text
Phase 0
建立基线
↓
Phase 1
统一 RequirementProfile
↓
Phase 2
统一 Product Type Router
↓
Phase 3
统一 Turn / Question Decision
↓
Phase 4
整理 Response 层
↓
Phase 5
Sales / Solution 去重复
↓
Phase 6
整理 Recommendation
↓
Phase 7
整理 RAG 边界
↓
Phase 8
整理 Calculation / Vision / Session
↓
Phase 9
测试重组
↓
Phase 10
删除确认后的 Legacy
↓
Phase 11
README / Docs 清理
```

---

# 27. 每阶段完成标准

每个阶段必须满足：

```text
功能不减少
+
现有推荐结果不发生非预期变化
+
测试通过
+
Regression Test 通过
+
没有新增重复逻辑
+
调用关系清晰
```

才能进入下一阶段。

---

# 28. 最终验收标准

本次重构完成后，重点不是“代码看起来更漂亮”，而是确认：

### 功能层

- LED 推荐正常
- LCD 推荐正常
- IFP 推荐正常
- RAG 正常
- 计算正常
- 图片识别正常
- First Contact 正常
- Session 正常
- API 正常

### 对话层

- 一轮只问一个主要问题
- 多条消息一次处理
- 不重复询问已经获得的信息
- 客户回答不同需求时仍然正确吸收
- 回复仍然自然
- 不因为代码整理增加额外 LLM 调用

### 架构层

- Requirement 只有一个核心模型
- Product Type 只有一个主要判断入口
- Turn Decision 只有一个主要决策入口
- Recommendation 只有一个主要执行入口
- RAG 不负责业务决策
- Sales / Solution 不重复维护需求状态
- Legacy 文件经过确认后再删除

---

# 29. 本次重构的核心原则

最终只遵循一句话：

> **不改业务，只整理职责；不先删除，先建立统一入口；不重写核心功能，只把重复代码收敛到唯一实现。**

任何一个重构动作如果无法证明：

```text
不会改变现有业务行为
```

就暂缓该动作，先增加测试和依赖分析。

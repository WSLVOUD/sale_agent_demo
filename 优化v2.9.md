# LED RAG 对话自然话术生成最终优化计划

版本：v2.0
日期：2026-09-23
仓库：`WSLVOUD/sale_agent_demo`
基准 Commit：`4a2e74016aead6539f1b4cf8b8ce49aaed5c6955`
基准状态：全量测试 `608 passed`

---

## 执行进度（2026-09-23 v2.9 轮；本次执行不提交、不推送）

> v2.9 这一轮把上表里标 🚧 的条目做掉了，逐条更新如下：
> **Phase 2/3 语义化 ✅、Phase 8 ✅、Phase 13 ✅、Phase 14 ✅、Phase 15 ✅、Phase 16/17/18 复核 ✅**；
> **Phase 7 的 21 个 next_action📍仍在进行**（已把"表达层动作"从 script_generator 里拿掉，见 Phase 8）。

| 计划条目 | 状态 | 落地内容 / 证据 |
|---|---|---|
| §四 Phase 1 QuestionSpec | ✅ 完成 | `QuestionSpec.anchor` 成为语义锚点（`text` 仅兼容别名，两者自动同步）+ `expression_anchor`；测试 `test_v28_plan_items.py::TestQuestionSpecAnchor` |
| §五/六/七 Phase 2 ResponseContext 清理 | ✅ 完成 | 默认业务限制自动注入（ask_only_one_question / do_not_invent_facts / do_not_repeat_known_facts）；`legacy_prose_fields()` 把仍是"成句话术"的字段（answer/opening/why）变成可观测；`prompt_block` 明确标注 "facts only — NOT final wording" |
| §八 Phase 3 最终 ResponseContext | ✅ 完成 | 结构化字段 + QuestionSpec + ResponseShape 齐备；legacy 成句字段已降级为"事实锚点"并带观测入口（删除动作留到 Phase 13 观测达标后） |
| §九 Phase 4 ResponseGenerator 结构化输入 | ✅ 完成 | 只吃结构化上下文（事实 + QuestionSpec + ResponseShape），不再接"完整回答 A/B/C" |
| §十 不用 Token 截断 | ✅ 完成 | 长度改由 ResponseShape + 语义任务范围控制 |
| §十一 语义任务范围 | ✅ 完成 | `ResponseShape.for_action()` + prompt 规则（完成当前动作 / 自然收尾） |
| §十二 Phase 5 ResponseShape | ✅ 完成 | 新增 `ResponseShape(action / allow_answer / allow_context / allow_question / max_questions)` 并进 prompt；测试 `TestResponseShape` |
| §十三 Phase 6 话术块拼接 | ✅ 完成 | Validator 新增 `multiple_response_blocks`；prompt 明确禁止拼接；测试 `test_multiple_response_blocks_are_flagged` |
| §十四/十五 Phase 7 script_generator 21 决策点 | 🚧 进行中（Step 2 已做一半） | 决策点数量仍钉死 21 处；本轮已把**表达层动作**从 script_generator 拿走（action_bridge），剩下的是 21 处分支级 `next_action` 语义迁移 |
| §十六 Phase 8 `_natural_reply` 不重新决策 | ✅ 完成 | 新增 `src/dialogue/action_bridge.py`：Policy 动作 → 表达层动作映射；`_natural_reply()` 改为 `expression_action(state, ...)` 并记录 `state["response_action"]`，不再按 answer/question 自己推断 |
| §十七 Phase 9 Planner/Flow 不重新设计 | ✅ 完成 | 已定为"候选 + 状态"并有源码不变量测试 |
| §十八 Phase 10 Validator 增强 | ✅ 完成 | 多问题（原有）+ 问错槽位 + 多个回复块 + 机械复述已知事实（≥3）+ 语义不完整；测试 `TestValidatorV28Checks` |
| §十九 Phase 11 失败处理 | ✅ 沿用 | LLM → Validator → Repair → Validator → Fallback 已存在 |
| §二十 Phase 12 Structured Fallback | ✅ 沿用 | `compose_from_context()` 仍只作 fallback |
| §二十一 Phase 13 legacy 观测 | ✅ 完成 | 新增 `src/dialogue/response_metrics.py`：total / llm_native / repair / structured_fallback / legacy_reply / validator_failures + `legacy_reply_ratio`；response_generator 与 script_generator 均已埋点，`/health.response_metrics` 可直接读 |
| §二十二 Phase 14 自然话术 Golden | ✅ 完成 | `tests/dialogue/golden/natural_cases.json`（5 例）+ `tests/dialogue/test_natural_response_golden.py`：每例检查问对 slot / 只问一个 / 不问已确认 / 不编参数型号 / 无多话术块 |
| §二十三 Phase 15 跨轮一致性测试 | ✅ 完成 | 新增 `tests/dialogue/test_cross_turn_question_consistency.py`：答过的 environment/size/viewing_distance 不再进候选；答非所问时 mall 信息照样入档 |
| §二十四 Phase 16 答非所问仍保存信息 | ✅ 覆盖 | `tests/dialogue/test_answer_wrong_slot.py` |
| §二十五 Phase 17 GroundedFact 保护 | ✅ 覆盖 | `src/rag/model_guard.py` + `grounded_facts` |
| §二十六 Phase 18 LLM 不判断"客户是否已答" | ✅ 覆盖 | 由 RequirementProfile + QuestionFlow + DialoguePolicy 决定 |
| §二十七 Phase 19 ResponseGenerator Prompt | ✅ 完成 | NATIVE_SYSTEM_PROMPT 已按清单补充（保留选定问题 / 最多一个问题 / 不编事实 / 不重复已知信息 / 一条连贯回复 / 禁止拼接） |
| §二十八 Phase 20 修改顺序 | 🚧 Step 1/2/3/4/5 完成 | Step 2 = 话术生成改为读 Policy 动作（action_bridge）✅；Step 5 = ResponseGenerator 唯一正常链 ✅；剩余 Step 6（21 决策点）与 Step 8（观测达标后删旧模板链） |

### §三十一 验收清单（v2.9 轮执行后）

```text
[x] ResponseContext 不再以完整销售话术作为主要输入
[x] QuestionSpec 成为唯一"本轮问什么"的定义
[x] QuestionSpec.slot 只能由 Python 决定
[x] QuestionSpec.intent 只能由 Python 决定
[x] QuestionSpec.anchor 只作为语义锚点
[x] LLM 只负责自然语言表达
[x] ResponseGenerator 是唯一正常客户文本出口
[x] 常见路径不再拼接多个完整话术块（Validator 检测 + 结构化上下文一次生成；剩余风险随 Phase 7 归零）
[x] 不使用 Token 截断控制正常回复长度
[x] Validator 能检测多问题 / 错误 Question Slot / 语义不完整 / unsupported facts / 明显重复 / 多个独立回复块
[x] Repair 失败才进入 fallback
[x] legacy_reply_path 可以统计（细分计数已补：response_metrics + /health）
[ ] script_generator 的 21 个决策点逐步迁移 ← 表达层已迁移，分支级 21 处待搬（数量已钉死）
[x] 跨轮不会重复询问已确认字段
[x] 客户答非所问时，仍然保存有效信息
[x] LLM 不参与 RequirementProfile / Recommendation / Calculation 决策
[x] 全量测试持续通过（本次执行后跑全量）
[x] Golden Dataset 中不存在明显话术拼接（tests/dialogue/golden 5 例全过）
```

### v2.9 轮新增/改动文件

```text
新增  src/dialogue/action_bridge.py                 （Phase 8：Policy 动作 → 表达层动作）
新增  src/dialogue/response_metrics.py              （Phase 13：回复链路计数，/health 可读）
新增  tests/dialogue/golden/natural_cases.json      （Phase 14：自然话术 Golden 数据）
新增  tests/dialogue/test_natural_response_golden.py（Phase 14）
新增  tests/dialogue/test_cross_turn_question_consistency.py（Phase 15）
新增  tests/dialogue/test_v29_plan_items.py         （Phase 2/3、8、13 的不变量）
改动  src/dialogue/response_context.py              （默认限制 + legacy 成句字段观测 + prompt 标注）
改动  src/dialogue/response_generator.py            （fallback 读 QuestionSpec + 计数埋点）
改动  src/agents/sales/nodes/script_generator.py    （_natural_reply 改读 Policy 动作 + legacy 计数）
改动  src/agents/sales/question_planner.py          （候选新增 dialogue_slot：Policy/QuestionSpec 用规范槽位名）
改动  src/api.py                                    （/health 暴露 response_metrics）
```

目标：

解决当前客户回复出现“完整话术块拼接、重复解释、旧问题残留、语气机械”的问题。

核心原则：

> Python 决定“这轮销售要完成什么”，LLM 只负责“怎么自然地表达”。

同时不使用 Token 截断作为正常回复长度控制手段。

---

# 一、当前代码结构审计结论

当前项目已经完成了一轮比较重要的架构收口：

```text
QuestionPlanner
      ↓
candidate_questions()
      ↓
QuestionFlow
      ↓
DialoguePolicy / DialogueAction
      ↓
ResponseContext
      ↓
ResponseGenerator
      ↓
ResponseValidator
```

当前 Commit `4a2e740` 已经完成：

* QuestionPlanner → 候选问题生成器
* QuestionFlow → 状态与问题计划管理
* QuestionSpec → 统一本轮问题定义
* ResponseGenerator → 唯一客户文本出口
* script_generator → 已禁止继续增加 pending_slot / pending_question
* legacy reply → 降级为 fallback
* legacy_reply_path → 已增加可观测标记
* 架构收敛测试 → 已建立
* 全量测试 → `608 passed`

因此本阶段：

**不要重新设计整个对话架构。**

重点是继续把现有架构真正收干净。

---

# 二、当前核心问题

当前问题不是：

```text
AI 不会写自然语言
```

而是：

```text
上游仍然可能生成完整话术
        ↓
多个模块产生完整句子
        ↓
ResponseContext 携带这些句子
        ↓
最终再次组合
        ↓
出现拼接感
```

例如：

```text
opening
+
answer
+
question
```

最终很容易变成：

```text
完整话术 A

完整话术 B

完整问题 C
```

即使最后交给 LLM 做 polish，也只是：

```text
拼接
→
重新润色
```

而不是：

```text
业务信息
→
一次自然表达
```

---

# 三、最终目标架构

```text
Customer Message
       ↓
RequirementProfile
       ↓
QuestionPlanner
       ↓
Candidate Questions
       ↓
DialoguePolicy
       ↓
DialogueDecision
       ↓
QuestionSpec
       ↓
ResponseContext
       ↓
ResponseGenerator
       ↓
LLM Natural Language Realization
       ↓
ResponseValidator
       ↓
Final Response
```

其中职责必须严格分开。

## Python 决定

```text
Action
Question Slot
Question Intent
业务事实
推荐结果
工程计算结果
禁止重复的信息
业务限制
```

## LLM 决定

```text
如何开头
如何连接上下文
如何组织句子
如何自然表达
如何结束
```

## Validator 决定

```text
是否违反业务边界
是否问错问题
是否问了多个问题
是否重复
是否编造事实
是否语义不完整
```

---

# 四、Phase 1：统一 QuestionSpec

当前已经存在：

```python
QuestionSpec(
    slot,
    intent,
    text
)
```

继续保留这个结构，但重新定义三个字段的职责：

```text
slot
↓
Python 决定

intent
↓
Python 决定

text
↓
只作为语义表达锚点
```

不要再把 `text` 当成最终客户问句。

例如不要：

```python
QuestionSpec(
    slot="viewing_distance",
    intent="collect viewing distance",
    text="Roughly how far will viewers typically stand from the screen?"
)
```

而应该逐步变成类似：

```python
QuestionSpec(
    slot="viewing_distance",
    intent="collect typical viewer distance",
    text="ask roughly how far viewers will be from the screen"
)
```

最终可以进一步改名：

```text
text
→
anchor
```

也就是：

```python
QuestionSpec(
    slot="viewing_distance",
    intent="collect typical viewer distance",
    anchor="ask roughly how far viewers will be from the screen"
)
```

这样可以从代码层面明确：

> 这里不是最终话术。

---

# 五、Phase 2：清理 ResponseContext

重点文件：

```text
src/dialogue/response_context.py
```

当前 ResponseContext 包含：

```text
question
question_slot
question_intent
required_question

answer
opening
why
notes

known_facts
missing_facts
business_goal
```

这些字段中有一部分容易被上游当成“最终话术”。

本阶段不建议立即全部删除。

先明确数据职责。

---

# 六、ResponseContext 新规范

允许：

```text
known_facts
newly_confirmed_fields
business_goal
question_spec
recommendation
engineering_result
recent_dialogue
recent_questions
```

可以保留但必须语义化：

```text
opening
answer
why
notes
```

这些字段只能描述：

```text
事实
意图
上下文
表达要求
```

不能保存完整销售段落。

---

# 七、禁止这种数据

不要出现：

```python
answer = "A 3 by 5 meter indoor screen is a solid size for a fixed install..."
```

然后：

```python
question = "Roughly how far will viewers be from the screen?"
```

最后：

```python
answer + question
```

这种方式必须逐步退出。

正确方式：

```python
known_facts = [
    "indoor",
    "3m × 5m"
]

business_goal = "collect viewing distance"

question_spec = QuestionSpec(
    slot="viewing_distance",
    intent="collect typical viewer distance",
    anchor="ask roughly how far viewers will be from the screen"
)
```

然后：

```text
ResponseGenerator
```

一次生成最终回复。

---

# 八、Phase 3：重新定义最终 ResponseContext

建议逐步收敛到：

```python
ResponseContext(
    action=...,

    customer_message=...,

    known_facts=[
        ...
    ],

    newly_confirmed_fields={
        ...
    },

    business_goal=...,

    question_spec=QuestionSpec(
        slot=...,
        intent=...,
        anchor=...
    ),

    recommendation=...,

    engineering_result=...,

    recent_dialogue=...,

    recent_questions=...,

    restrictions=[
        "ask_only_one_question",
        "do_not_invent_facts",
        "do_not_repeat_known_facts"
    ],

    language=...
)
```

核心目标：

```text
ResponseContext = 业务上下文
```

而不是：

```text
ResponseContext = 已经拼好的销售话术
```

---

# 九、Phase 4：重构 ResponseGenerator

重点文件：

```text
src/dialogue/response_generator.py
```

当前已经有 Native Generation，这是正确方向。

下一步要确保：

```text
ResponseGenerator
```

接收的是：

```text
结构化上下文
```

而不是：

```text
完整回答 A
+
完整回答 B
+
完整问题 C
```

---

# 十、不使用 Token 截断控制长度

本项目不建议依赖：

```text
max_output_tokens
```

解决长回复。

因为：

```text
Token 限制
↓
输出被截断
↓
可能出现半句话
↓
语义不完整
```

因此 Token 上限最多只能作为极端情况下的保险，不能作为正常话术长度控制方案。

---

# 十一、使用“语义任务范围”控制长度

例如：

```text
Action:
ANSWER_AND_ASK

Known Facts:
- LED
- indoor
- 3m × 5m
- 5m viewing distance

Business Goal:
understand usage scenario

Question:
application
```

LLM 的任务：

```text
完成当前业务动作
```

而不是：

```text
尽可能解释更多内容
```

要求：

```text
Complete the current business action.

Preserve the selected question.

Ask only the selected question.

Do not introduce another question.

Do not invent facts.

Do not repeat known information unnecessarily.

Produce one coherent customer-facing response.

Stop naturally when the current action is complete.
```

这样控制长度的核心是：

```text
Action
+
Context
+
QuestionSpec
```

而不是：

```text
Token 数
```

---

# 十二、Phase 5：增加 ResponseShape

建议增加一个轻量级结构：

```python
ResponseShape
```

作用不是限制 token，而是告诉 LLM：

> 这一轮允许表达哪些内容。

例如：

```python
ResponseShape(
    action="ASK",
    allow_answer=False,
    allow_context=True,
    allow_question=True,
    max_questions=1
)
```

对于：

```text
ANSWER_AND_ASK
```

可以：

```python
ResponseShape(
    action="ANSWER_AND_ASK",
    allow_answer=True,
    allow_context=True,
    allow_question=True,
    max_questions=1
)
```

这样 AI 不需要自己猜：

```text
这次应该写多少内容？
```

而是已经知道：

```text
这次只需要完成一个回答 + 一个问题。
```

---

# 十三、Phase 6：彻底解决“话术块拼接”

当前禁止：

```text
opening
+
answer
+
question
```

最终必须变成：

```text
Structured Context
        ↓
ResponseGenerator
        ↓
一次 LLM Generation
        ↓
One coherent response
```

例如 Python 给：

```json
{
  "action": "ANSWER_AND_ASK",
  "known_facts": [
    "LED",
    "indoor",
    "3m × 5m",
    "5m viewing distance"
  ],
  "business_goal": "understand usage scenario",
  "question": {
    "slot": "application",
    "intent": "identify the main use case"
  }
}
```

LLM 自己组织成：

```text
That gives me a clear picture of the basic setup. What will the screen mainly be used for, such as a meeting room, retail space, lobby, or another application?
```

而不是：

```text
固定回答 A
+
固定回答 B
+
固定问题 C
```

---

# 十四、Phase 7：继续收敛 script_generator

重点文件：

```text
src/agents/sales/nodes/script_generator.py
```

当前已经通过测试把：

```text
state["next_action"] =
```

控制在 21 个决策点。

下一步：

**不要继续在 script_generator 中增加新的业务决策。**

应该逐个迁移：

```text
script_generator
        ↓
只准备业务上下文
        ↓
DialoguePolicy
        ↓
DialogueDecision
```

---

# 十五、21 个 next_action 决策点迁移方式

不要一次性全部删除。

按照业务类型逐个迁移：

```text
ask / ask_only
    ↓
ASK / ANSWER_AND_ASK

trigger_solution
    ↓
RECOMMEND

product_question
    ↓
DIRECT_ANSWER / ANSWER_AND_ASK

ordinary answer
    ↓
DIRECT_ANSWER

closing
    ↓
ACK_ONLY
```

每迁移一批：

```bash
python -m pytest -q
```

确保：

```text
608 passed
```

基线不下降。

---

# 十六、Phase 8：处理 _natural_reply()

重点关注：

```text
_natural_reply()
```

当前这个函数仍然有一定的：

```text
Action 推断
Question 推断
```

例如从：

```text
answer
question
```

重新判断：

```text
ANSWER_AND_ASK
DIRECT_ANSWER
ASK
```

这属于重复决策。

最终目标：

```text
DialoguePolicy
        ↓
已经确定 Action
        ↓
_natural_reply()
        ↓
只构造 ResponseContext
        ↓
ResponseGenerator
```

也就是说：

> `_natural_reply()` 不再重新决定业务动作。

---

# 十七、Phase 9：QuestionPlanner 和 QuestionFlow 不要重新设计

当前：

```text
QuestionPlanner
    ↓
candidate_questions()
```

以及：

```text
QuestionFlow
    ↓
状态 + 计划
```

这部分方向正确。

不要改成：

```text
QuestionPlanner → LLM
```

因为这样会把已经完成的确定性决策重新交给 AI。

正确关系：

```text
QuestionPlanner
    ↓
Candidate Questions
    ↓
DialoguePolicy
    ↓
Final Question Slot
    ↓
ResponseGenerator
    ↓
Natural Wording
```

---

# 十八、Phase 10：增强 ResponseValidator

重点文件：

```text
src/dialogue/response_validator.py
```

增加以下检查。

## 1. Question Count

如果当前：

```text
max_questions = 1
```

那么：

```text
0 → 根据 Action 判断是否允许
1 → 正常
>1 → fail
```

---

## 2. Question Slot

Python：

```text
slot = application
```

AI：

```text
What is your viewing distance?
```

必须：

```text
fail
```

---

## 3. 已知事实重复

客户已经提供：

```text
indoor
3m × 5m
5m viewing distance
```

AI 不应该机械重复：

```text
You have a 3m × 5m indoor screen
with a 5m viewing distance...
```

除非这些信息对当前回答确实必要。

---

## 4. Unsupported Fact

如果 Context 没有：

```text
P2.5
HDR
waterproof
high brightness
```

AI 不应该自行添加。

出现：

```text
fail
```

---

## 5. Multiple Response Blocks

检测：

```text
第一套完整销售话术

第二套完整销售话术

第三套完整问题
```

这种情况。

不是简单禁止所有换行，而是判断：

> 是否出现多个独立业务回复块。

---

## 6. Semantic Completeness

检测：

```text
句子是否明显没有完成
业务动作是否没有完成
```

例如：

```text
We can recommend...
```

然后突然结束。

必须：

```text
fail
```

---

# 十九、Phase 11：失败处理机制

不要：

```text
输出太长
↓
直接截断
```

而应该：

```text
LLM
 ↓
Validator
 ↓
Fail
 ↓
Repair
 ↓
Validator
 ↓
Pass → Return
Fail → Structured Fallback
```

当前项目已经有：

```text
_repair_response()
```

因此不需要重新设计整个机制。

只需要增强 Validator。

---

# 二十、Phase 12：Structured Fallback 保留

当前：

```text
compose_from_context()
```

继续保留。

但是定位必须是：

```text
正常：
ResponseGenerator → LLM

异常：
LLM失败
    ↓
Repair失败
    ↓
Structured Fallback
```

不要让 fallback 再次成为正常主路径。

---

# 二十一、Phase 13：继续退出 legacy reply chain

当前：

```text
legacy_reply_path
```

已经存在。

继续观察：

```text
total_turns
llm_native_turns
repair_turns
structured_fallback_turns
legacy_reply_turns
validator_failures
```

重点观察：

```text
legacy_reply_path == True
```

当生产环境长期接近：

```text
0
```

再删除：

```text
compose_requirement_reply()
```

以及旧模板链。

不要一次性删除。

---

# 二十二、Phase 14：增加自然话术 Golden Dataset

当前已经有 Golden Dataset / autoeval 基础。

进一步增加：

```text
tests/dialogue/golden/
```

测试重点不再只是：

```text
有没有问对 slot
```

还要测试：

```text
是不是一条自然完整的销售回复
```

例如：

```text
Case 001

Customer:
3m x 5m indoor LED

Expected Action:
ANSWER_AND_ASK

Expected Slot:
viewing_distance
```

检查：

```text
问的是 viewing_distance
没有问第二个问题
没有重复问 environment
没有编造 pixel pitch
没有出现多个独立话术块
```

---

# 二十三、Phase 15：增加跨轮一致性测试

这个测试非常重要。

例如：

第一轮：

```text
Customer:
3m x 5m indoor LED
```

系统：

```text
Question:
viewing_distance
```

第二轮：

```text
Customer:
5 meters
```

系统：

```text
不能再次问 viewing_distance
```

必须使用新的：

```text
QuestionSpec
```

---

# 二十四、Phase 16：测试客户答非所问

你的项目有一个非常重要的业务要求：

> 客户回答的问题可能不是当前系统正在问的问题，但只要客户提供了有效需求信息，就必须保存。

因此必须测试：

```text
AI:
What is the viewing distance?

Customer:
It will be installed in a shopping mall.
```

系统应该：

```text
保存 application / location-related information
```

然后：

```text
重新计算 RequirementProfile
```

再决定下一问题。

不能因为：

```text
客户没有回答 viewing_distance
```

就丢掉：

```text
shopping mall
```

最终：

```text
RequirementProfile
        ↓
DialoguePolicy
        ↓
new QuestionSpec
        ↓
ResponseGenerator
```

---

# 二十五、Phase 17：保护事实来源

继续使用当前项目的：

```text
GroundedFact
```

LLM 可以表达：

```text
customer facts
retrieved facts
calculated facts
recommendation facts
```

但不能自己创造事实来源。

例如：

Context 没有：

```text
P2.5
```

LLM 就不能自己写：

```text
P2.5 would be ideal.
```

推荐仍然必须由：

```text
Recommendation Engine
```

决定。

---

# 二十六、Phase 18：LLM 不负责判断客户是否已经回答

这是整个架构中非常重要的一条。

LLM 不负责：

```text
客户是否已经回答 viewing_distance
```

由：

```text
RequirementProfile
+
QuestionFlow
+
DialoguePolicy
```

判断。

LLM 只负责：

```text
如何表达已经确定的结果。
```

否则会出现：

```text
Customer:
5m

LLM:
I still need to know the viewing distance...
```

这种业务错误。

---

# 二十七、Phase 19：最终 ResponseGenerator Prompt

核心 Prompt 建议：

```text
The business decision has already been made.

Your task is only to express that decision naturally.

You must:
- complete the current business action
- preserve the selected question topic
- preserve grounded facts
- ask at most one question
- never introduce another question
- never invent facts
- never repeat known information unnecessarily
- never ask for information already confirmed
- produce one coherent customer-facing response
- stop naturally when the current action is complete

Do not:
- concatenate independent response blocks
- repeat the same information
- add another question
- change the selected question slot
- change the business action
- explain internal reasoning
- mention system fields
- use canned sales language
```

核心思想：

```text
AI 不负责决策。

AI 只负责语言实现。
```

---

# 二十八、Phase 20：最终代码修改顺序

必须严格按照以下顺序。

## Step 1

修改：

```text
src/dialogue/response_context.py
```

完成：

```text
QuestionSpec
ResponseContext
```

语义收口。

---

## Step 2

修改：

```text
src/agents/sales/nodes/script_generator.py
```

减少：

```text
完整销售话术生成
```

改成：

```text
业务事实 + Action + QuestionSpec
```

---

## Step 3

修改：

```text
src/dialogue/response_generator.py
```

实现：

```text
Structured Context
→
One Natural Generation
```

---

## Step 4

修改：

```text
src/dialogue/response_validator.py
```

增加：

```text
Question Count
Question Slot
Semantic Completeness
Repeated Facts
Unsupported Facts
Multiple Response Blocks
```

---

## Step 5

修改：

```text
src/dialogue/response_coordinator.py
```

确保：

```text
ResponseContext
        ↓
ResponseGenerator
```

成为唯一正常文本链。

---

## Step 6

逐个迁移：

```text
script_generator.py
```

中的 21 个：

```text
next_action
```

决策点。

---

## Step 7

观察：

```text
legacy_reply_path
```

---

## Step 8

最后删除：

```text
compose_requirement_reply
```

及旧模板链。

---

# 二十九、测试要求

每完成一个 Phase：

```bash
python -m pytest -q
```

基线：

```text
608 passed
```

要求：

```text
架构改造不能破坏现有业务测试。
```

如果测试数量下降：

```text
先定位原因
再继续改造
```

不能为了架构收口直接删除失败测试。

---

# 三十、新增测试分类

建议逐步增加：

```text
tests/dialogue/

test_action_consistency.py

test_architecture_convergence.py

test_response_generator.py

test_response_validator.py

test_question_flow.py

test_question_planner.py

test_natural_response_golden.py

test_cross_turn_question_consistency.py
```

---

# 三十一、最终验收标准

必须满足：

```text
[ ] ResponseContext 不再以完整销售话术作为主要输入

[ ] QuestionSpec 成为唯一“本轮问什么”的定义

[ ] QuestionSpec.slot 只能由 Python 决定

[ ] QuestionSpec.intent 只能由 Python 决定

[ ] QuestionSpec.anchor 只作为语义锚点

[ ] LLM 只负责自然语言表达

[ ] ResponseGenerator 是唯一正常客户文本出口

[ ] 普通路径不再拼接多个完整话术块

[ ] 不使用 Token 截断控制正常回复长度

[ ] Validator 能检测多问题

[ ] Validator 能检测错误 Question Slot

[ ] Validator 能检测明显语义不完整

[ ] Validator 能检测 unsupported facts

[ ] Validator 能检测明显重复

[ ] Validator 能检测多个独立回复块

[ ] Repair 失败才进入 fallback

[ ] legacy_reply_path 可以统计

[ ] script_generator 的 21 个决策点逐步迁移

[ ] 跨轮不会重复询问已确认字段

[ ] 客户答非所问时，仍然保存有效信息

[ ] LLM 不参与 RequirementProfile 决策

[ ] LLM 不参与 Recommendation Engine 决策

[ ] LLM 不参与 Calculation 决策

[ ] 全量测试持续通过

[ ] Golden Dataset 中不存在明显话术拼接
```

---

# 三十二、最终架构

最终要达到：

```text
                    Customer
                       │
                       ▼
              RequirementProfile
                       │
                       ▼
               QuestionPlanner
                       │
                       ▼
              Candidate Questions
                       │
                       ▼
                DialoguePolicy
                       │
                       ▼
                DialogueAction
                       │
                       ▼
                 QuestionSpec
                       │
                       ▼
               ResponseContext
                       │
          ┌────────────┴────────────┐
          │                         │
          │      Structured         │
          │        Facts            │
          │                         │
          └────────────┬────────────┘
                       ▼
              ResponseGenerator
                       │
                       ▼
                     LLM
                       │
                       ▼
              ResponseValidator
                       │
              ┌────────┴────────┐
              │                 │
             PASS              FAIL
              │                 │
              │              Repair
              │                 │
              │             Validator
              │                 │
              └────────┬────────┘
                       ▼
                Final Response
                       │
                       ▼
                   Customer
```

---

# 三十三、最终核心原则

整个改造只需要坚持三句话：

```text
第一：
Python 决定“这轮要完成什么”。

第二：
LLM 决定“这件事怎么自然地说”。

第三：
Validator 决定“LLM 有没有越界”。
```

因此不要采用：

```text
完整话术 A
+
完整话术 B
+
完整问题 C
+
LLM Polish
```

而应该采用：

```text
Action
+
Facts
+
QuestionSpec
+
Business Goal
        ↓
ResponseGenerator
        ↓
一条完整自然回复
        ↓
ResponseValidator
```

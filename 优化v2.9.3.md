LED / LCD / IFP 首层产品判断与需求链路重构计划

一、改造目标

==================================================
执行进度（2026-09-23；本次执行不提交、不推送）
==================================================

| 阶段 | 内容 | 状态 | 落地 / 证据 |
|---|---|---|---|
| 三 一阶段 | 梳理代码 | ✅ 完成 | LED 判断入口=`turn_kind`+`product_type_router`；display_type 写入=需求抽取/档案；LCD·IFP 数据=三份 JSON；旧 LCD/IFP 链路=Solution 的 others/product_question 分支（本层不再调用） |
| 四 二阶段 | 建立 Product Type Router | ✅ 完成 | 新增 `src/dialogue/product_type_router.py`：第一层只有 LED / LCD / UNKNOWN（**没有 IFP**） |
| 五 三阶段 | 判断优先级 | ✅ 完成 | ① 明确 LED ② 明确 LCD ③ IFP 特征→LCD(subtype=IFP) ④ 图片(Vision) ⑤ 用途推断 ⑥ 不知道/解释 ⑦ 信息不足询问；最终 fallback=LED(DEFAULT) |
| 六 四阶段 | 只知道用途 → AI 辅助判断 | ✅ 完成 | `_infer_from_purpose()`：给倾向 + `ask_customer=True`（必须客户确认，不把选择丢回客户） |
| 七 五阶段 | 场景不能机械关键词 | ✅ 完成 | 组合判断：触控/手写→LCD(IFP)；户外/广告/大型场馆→LED；超大尺寸或 ≥6m 远距离→LED；普通会议室→LCD 倾向；**场景只是证据** |
| 八 六阶段 | 图片输入流程 | 🚧 部分 | 路由器已按 §六 处理 Vision 结果：INFERRED/VISION + 要确认 / 否认则重判 / "不知道"则采用 AI 判断 / 无法判断不硬猜。**Vision 侧输出 display_type 还没做**（现有视觉只抽尺寸/环境等） |
| 九 七阶段 | 客户不知道 LED/LCD 是什么 | ✅ 完成 | `needs_explanation` + `explanation_lines()`（LED：可做大/基本无拼缝/室内外都行；LCD：像商用液晶/可拼接但有拼缝/主要室内）→ Greeting 时先解释再问 |
| 十 八阶段 | 自然对话（不问卷） | ✅ 完成 | 沿用单一问题约束（ResponseShape/Validator）+ 先接住客户；不连续抛问 |
| 十一 九阶段 | 不许为自然编造 | ✅ 完成 | 路由器只用客户原话/档案/图片结果；ResponseContext 带 grounded facts，Validator 拦编造 |
| 十二 十阶段 | 产品类型状态管理 | ✅ 完成 | `display_type / subtype / status / source / confidence / reason / locked / evidence`，跨轮持久化（memory store） |
| 十三 十一阶段 | 类型锁定 | ✅ 完成 | 客户确认 → locked；普通信息（会议室/室内/大屏）不改类型；只有明确改口才切换 |
| 十四 十二阶段 | LED 链路保持不动 | ✅ 完成 | 未改 RequirementProfile / Gate / Engine / Pitch / Brightness / Environment / Installation / Viewing distance / Size |
| 十五 十三阶段 | LCD 链路重新建立 | 🚧 入口 + 留白 | LCD_ENTRY + LCDPolicy（v2.9.2 已建）；具体需求链按计划"暂留白"，未继承旧逻辑 |
| 十六 十四阶段 | IFP 作为 LCD 子类型 | ✅ 完成 | 首层输出 LCD + `subtype=IFP`；`product_type_router` 永不返回 IFP 作为类型 |
| 十七 十五阶段 | 三份数据继续保持独立 | ✅ 完成 | led/lcd/ifp_products.json 未合并；路由只决定进哪条链 |
| 十八 十六阶段 | 旧 LCD/IFP 链路隔离 | ✅ 完成（隔离与标记，未删） | 首层只产出 LED_ENTRY / LCD_ENTRY / PRODUCT_SELECTION，不调用旧 LCD/IFP 分支；旧代码保留待新链稳定后删除 |
| 十九 十七阶段 | 多消息聚合 | ✅ 完成 | 沿用 Turn 引擎；路由器吃聚合后的整句，一次判断 |
| 二十 十八阶段 | 测试场景 | ✅ 完成 | 新增 `tests/dialogue/test_v293_product_type_router.py`（18 条，覆盖 17 个场景） |

测试计划逐条对照（§二十）

```text
[x] 1  LED display            → LED（CONFIRMED/CUSTOMER/locked）
[x] 2  LCD display            → LCD
[x] 3  interactive display    → LCD + subtype IFP（要确认）
[x] 4  touch screen meetings  → LCD + subtype IFP
[x] 5  "I need a display."    → UNKNOWN → 自然询问（不硬猜）
[x] 6  outdoor advertising    → LED（INFERENCE，要确认）
[x] 7  meeting room           → 分析用途 → 给倾向 → 要确认
[x] 8  meeting room + write   → LCD + subtype IFP
[x] 9  huge + 10m viewing     → LED 倾向 → 要确认
[x] 10 图片判断 LED            → INFERRED/VISION → 要确认
[x] 11 图片判断 LCD            → INFERRED/VISION → 要确认
[x] 12 图片无法判断            → UNKNOWN → 询问（不猜）
[x] 13 AI 判断 LED + "I don't know" → 采用 AI 判断 → 锁定
[x] 14 不知道 LED/LCD 是什么    → 先简单解释 → 再问
[x] 15 不知道选哪个            → AI 按用途建议 → 客户确认
[x] 16 明确 LED 后说 meeting room → 仍然 LED（锁定）
[x] 17 明确 LED 后 "Actually, I want LCD." → 切换 LCD
```

最终核心决策规则（§二十一）/ 工程目标（§二十二）落实对照

```text
[x] 1. LED 现有成熟链路不动
[x] 2. 第一层只负责 LED/LCD 分流
[x] 3. IFP 属于 LCD（首层不出 IFP）
[x] 4. LCD/IFP 数据库继续分开（三份 JSON）
[x] 5. 图片由 Vision 辅助判断（路由侧已就绪；Vision 侧 display_type 待做）
[x] 6/7. 客户不知道 → 先解释；只知道用途 → AI 主动帮助选型
[x] 8. AI 推断需要客户确认（除非客户明确"不知道，你决定"）
[x] 9. 最终无法判断才默认 LED（DEFAULT，且默认判断不返回 DEFAULT）
[x] 10/11. 已确认锁定；客户明确修改才允许切换
[x] 12/13. 旧 LCD/IFP 链路先隔离（新层不调用），新链稳定后再删
[x] 14. 多条消息聚合后一次处理
[x] 15. 不许为自然编造客户没提供的信息
[x] 16/17. 先接住客户 → 再判断 → 自然推进；避免连续提问
[x] 18. Product Type Router 只决定进哪条链，不做型号推荐
```

本轮改动文件

```text
新增  src/dialogue/product_type_router.py            （首层产品判断 + 状态机 + 锁定 + 解释文案）
新增  tests/dialogue/test_v293_product_type_router.py（18 条，覆盖 17 场景）
改动  src/agents/sales/nodes/classify.py             （接入路由器 + 跨轮锁定 + 需求簿按 LED/LCD 记录）
改动  src/agents/sales/nodes/script_generator.py     （"LED/LCD 是什么"先解释再问）
改动  src/memory/store.py                            （display_type_decision 持久化）
```

返工记录：`script_generator` 里为了取语言规则写成 `from ....rag.reply_composer import reply_language`，
把 `reply_language` 变成了整个函数的局部变量，导致同函数其它分支 `UnboundLocalError`
（3 条用例当场红）。已改为 `import ... as _reply_language`，全量恢复。

第三轮修正（客户实测：`i need a screen` → AI 仍然"An LED screen makes sense…"）

**根因：项目里有多处"没点名就默认 LED"的隐藏默认值**，路由器建好了也绕不过它们：

| 位置 | 旧行为 | 现在 |
|---|---|---|
| `query_understanding._DISPLAY_TYPE_KEYWORDS["LED"]` | 把 **"显示屏 / 屏幕 / 屏 / display / screen / pantalla / écran / экран"** 都算 LED | 只保留 LED 专有词（`led` / `led屏` / `点间距屏` / `箱体屏`） |
| `query_understanding._rule_slots()` | 有"型号 / 点间距 / 场景 / 室内外"等销售上下文就 `display_type = "LED"` | 删除：类型未点名就是未知，交给 Product Type Router |
| `query_understanding` 检索式改写 | `slots.get("display_type") or "LED"` | 未定时不写类型（只写 "display"） |
| `turn_kind.detect_product_domain()`（v2.9.1 加的） | 泛指"要一块屏" → LED | 删除：只说 screen/display → UNKNOWN |

**并且把"类型确认"做成硬闸门**（`script_generator`，仅对 need_query/industry 生效，
价格/交期/闲聊等其它分支不受影响）：

```text
display_type 未 CONFIRMED
    → 这一轮只解决"LED 还是 LCD"（推断→是的/否确认；信息不足→直接问）
    → 客户确认后锁定，再进对应链路；LED 只在最终 fallback 时才用
display_type == LCD（已确认）
    → 进 LCD_ENTRY，只回一句自然的承接（计划 §十五 需求链留白），**不套用 LED 需求问题**
```

另外补上"确认/锁定"链路（计划 §六/§十一）：客户回 `yes / 好的 / 可以` 且上一轮是我们
给的类型建议 → 采用并锁定（`CONFIRMED / CUSTOMER / locked=true`）。

**真机验证（走本地代理直连 DeepSeek，production 路径）**

```text
客户: i need a screen
  type=UNKNOWN status=UNKNOWN entry=PRODUCT_SELECTION
  AI : A screen could mean a few different things on our side, so let's narrow it down
       before I point you anywhere. Are you after an LED display, or an LCD option
       like a video wall or interactive flat panel?          ← 不再默认 LED

客户: i need a screen for a meeting room
  type=LCD status=INFERRED
  AI : A meeting room screen is a pretty common request, and for that kind of setup
       I'd usually point people toward LCD first. ... Want me to go ahead with LCD for you?
                                                             ← 推断 + 是/否确认（不是菜单）
客户: yes
  type=LCD status=CONFIRMED locked=True entry=LCD_ENTRY
  AI : Understood, let's go with LCD video wall for this project. ← 锁定 + 进 LCD 入口
```

**因行为改变而同步更新的既有断言（不是删测试）**

- `test_v291_others_cleanup::test_domains_are_detected`：只说 "outdoor screen for a stadium"
  → `turn_kind` 给 UNKNOWN，**LED 倾向由 Product Type Router 推断**（INFERRED）
- `test_vision_pipeline::TestVisionConfirmation`：带图那一轮先确认"图片看到什么 + 产品类型"，
  需求细节（installation）留到类型确认之后（计划 §六 + 一轮一个问题）
- `test_session_reset`：重置后不再默认 LED（类型未定照实为 None）

全量（第三轮修正后）：`python -m pytest -q` → **676 passed**。

第四轮修正（客户实测：`i need a display` → AI 仍在说 "…cabinet design and brightness…
where it's going to live"）

**先查清楚是哪一种情况（都跑了真机复现）**

| 场景 | 现在的实际行为 |
|---|---|
| 全新会话 + `i need a display` | `type=UNKNOWN` → AI 问 "Are you after an LED display, or something on the LCD side…?" ✅ **已符合 §五** |
| 同一会话：先说 `i need a led display`，再说 `i need a display` | `type=LED CONFIRMED locked=True` → 继续按 LED 链路问需求 ✅ 这是计划 §十一/§十三 要求的"已确认就锁定" |

所以客户看到的 LED 式回复，只可能来自两种情况：

1. **服务还在跑旧进程**（没重启 uvicorn）—— 旧代码里根本没有类型闸门；
2. 该会话**之前已经确认过 LED**（例如说过 "i need a led display"）→ 按计划锁定 LED。

**为避免再出现"说不清是哪一种"，这次补了两件事**

1. **收口层强约束**（`src/orchestrator.py::_finalize_turn_response`）：
   只要销售层给出过类型判断且 `status != CONFIRMED`，**任何路径**（conversation / free
   question / product question / need_query）都不能把需求细节问题发出去 ——
   收口层会把需求问题换成"LED 还是 LCD"的确认问题（正文里那句旧问句也一起换掉）。
   这样"默认 LED 的需求问题"在结构上无法漏出。
2. **启动自检日志**（`src/api.py`）：服务起来时会打印
   `[Build] ProductTypeRouter=enabled（probe 'i need a display' → UNKNOWN/UNKNOWN）；greeting_merge=True…`
   —— 一眼就能确认"跑的是不是新代码"；如果这行没有，就是没重启。

回归：`tests/dialogue/test_v293_product_type_router.py` 增加收口层拦截用例
（类型未确认 + 待问 environment → 最终问 display_type，且回复里不出现 indoors/outdoors）。

全量（第四轮修正后）：`python -m pytest -q` → **677 passed**。

在现有项目中增加独立的 Product Type Router，负责客户进入后的第一层产品类型判断。

核心目标：

1. 客户第一次发完消息后，先执行现有 First Contact 建联流程。
2. 建联完成后，先判断客户需要 LED 还是 LCD。
3. 客户明确说 LED → 直接进入目前已经成熟的 LED 需求链路。
4. 客户明确说 LCD → 进入新的 LCD 需求链路。
5. IFP 不作为第一层独立产品类型，而是 LCD 的一个子类型。
6. 客户没有明确说 LED/LCD，但知道自己的使用目的 → AI 根据场景和需求辅助判断。
7. AI 判断出 LED/LCD 后，用自然的方式向客户确认。
8. 客户不知道 LED/LCD 是什么 → AI 简单解释后再让客户选择。
9. 客户发送图片 → Vision 判断屏幕类型，再向客户确认。
10. 客户不知道自己应该选择什么 → AI 根据用途帮助客户选型。
11. 完全没有足够信息时，保留 LED 作为最终 fallback。
12. 产品类型一旦被客户明确确认，就锁定。
13. 客户明确修改产品类型时，允许重新路由。
14. 旧 LCD/IFP 链路先隔离，不与新链路同时运行。
15. 新 LCD/IFP 链路验证稳定后，再删除旧链路。
16. 整个过程保持自然销售对话，避免问卷式、机器人式话术。

二、最终整体架构

客户第一次消息
↓
First Contact
↓
Requirement Extraction
↓
文字 / 图片 / 文字+图片理解
↓
Product Type Router
↓
┌──────────────────────────────┐
│                              │
LED                            LCD
│                              │
↓                              ↓
现有成熟 LED 链路              新 LCD 链路
                               │
                               ├── 普通 LCD
                               │
                               └── IFP

如果无法确定：
↓
继续理解客户用途
↓
AI 辅助判断
↓
客户确认
↓
进入对应链路


三、第一阶段：梳理当前项目代码

重点检查：

src/rag/query_understanding.py
src/core/requirement_extractor.py
src/agents/sales/nodes/requirement.py
src/agents/solution/nodes/requirement.py
src/rag/readiness.py
src/rag/recommendation_engine.py
src/agents/sales/question_planner.py
ConversationState / RequirementProfile
LCD 产品数据
IFP 产品数据
LED 产品数据

重点确认：

1. LED 判断入口。
2. LCD 判断入口。
3. IFP 判断入口。
4. 默认 LED 逻辑。
5. display_type 写入位置。
6. display_type 修改位置。
7. LCD 推荐入口。
8. IFP 推荐入口。
9. 旧 LCD/IFP 链路完整调用路径。
10. 哪些逻辑属于辅助识别，哪些属于真正需求链。

原则：

不要直接删除旧代码。

先梳理、隔离、标记 legacy。


四、第二阶段：建立 Product Type Router

新增独立产品类型判断层。

第一层只负责：

“客户应该进入 LED 还是 LCD。”

第一层产品类型只有：

LED
LCD
UNKNOWN

不要在第一层出现：

IFP

因为：

IFP 属于 LCD。

最终关系：

LED

LCD
├── 普通 LCD
└── IFP


五、第三阶段：产品类型判断优先级

第一优先级：

客户明确说 LED
→ LED

第二优先级：

客户明确说 LCD
→ LCD

第三优先级：

客户明确表达 IFP 特征：

Interactive Display
Interactive Panel
Touch Display
Touch Screen
Whiteboard
Interactive Whiteboard
电子白板
交互平板
触控
手写

→ LCD
→ 后续 LCD 链路判断 IFP

第四优先级：

客户发送图片
→ Vision 判断 LED / LCD
→ 向客户确认

第五优先级：

客户没有明确 LED/LCD，但是描述了使用目的
→ 分析场景
→ 分析需求
→ 判断更适合 LED 还是 LCD
→ 给出简单自然的建议
→ 向客户确认

第六优先级：

客户不知道 LED/LCD 的区别
→ 简单解释
→ 再询问客户

第七优先级：

没有足够信息判断
→ 自然询问 LED/LCD

最终 fallback：

所有信息都不足，且无法继续获得有效信息
→ 默认 LED

注意：

默认 LED 只能作为最后 fallback。

不能设计成：

“客户没有说 LCD → 默认 LED”。


六、第四阶段：客户知道用途，但是不知道选择什么

这是本次改造的重要分支。

例如：

客户：

“I need a screen for a meeting room.”

但客户没有说 LED / LCD / IFP。

不要立即问：

“LED or LCD?”

应该先分析客户用途。

分析：

场景
环境
尺寸
观看距离
安装方式
是否需要触控
是否需要手写
是否需要拼接
是否户外
是否大型显示
其他客户已经提供的信息

然后：

形成产品类型倾向
↓
LED / LCD
↓
用自然语言告诉客户建议
↓
让客户确认

例如：

“Got it. For this type of application, I’d suggest looking at LED options first. Would you like me to continue with LED?”

不能直接把客户不知道的选择重新丢给客户。


七、第五阶段：场景判断不能机械关键词匹配

不能简单设计：

会议室 = IFP
室内 = LCD
户外 = LED
广告 = LED

应该综合：

场景
+
环境
+
尺寸
+
观看距离
+
安装方式
+
是否触控
+
是否手写
+
是否拼接
+
其他明确需求

例如：

会议室
+
手写 / 触控
→ LCD
→ IFP

会议室
+
超大尺寸
+
远距离观看
→ 可能更倾向 LED
→ 向客户确认

户外
+
广告
→ LED

室内
+
普通视频墙
→ 可能 LED，也可能 LCD
→ 根据其他需求继续判断

场景只是证据，不应该覆盖客户明确选择。


八、第六阶段：图片输入流程

客户直接发送图片：

客户图片
↓
Vision Model
↓
提取显示产品相关信息
↓
判断：

LED
LCD
UNKNOWN

↓
生成判断依据
↓
自然向客户确认

如果 AI 判断 LED：

“From the image, it looks like an LED display. Is this the type of screen you’re looking for?”

客户确认：

→ LED
→ 锁定
→ 进入 LED 成熟链路

客户否认：

→ 不继续强行使用原判断
→ 重新分析 / 询问客户

客户说：

“I don't know.”

→ 使用 AI 当前判断
→ 锁定 LED/LCD
→ 进入对应链路

Vision 无法判断：

→ 不允许强行猜测
→ 继续询问或根据客户用途辅助判断。


九、第七阶段：客户不知道 LED/LCD 是什么

如果客户问：

“What is LED?”
“What is LCD?”
“I don't know.”

不要输出技术文档式解释。

只提供简单业务解释：

LED：

可以做很大的显示屏，基本没有传统 LCD 拼接边框，室内和室外都可以使用。

LCD：

类似液晶电视/显示器，可以进行拼接，但屏幕之间会有拼接边框，主要用于室内。

然后自然继续：

“If you tell me what you plan to use the screen for, I can also help you choose between them.”

如果客户愿意描述用途：

→ AI 根据用途帮助判断。

如果客户要求 AI 直接选择：

→ AI 给出建议
→ 向客户确认
→ 锁定。


十、第八阶段：自然对话处理

Product Type Router 不能做成问卷系统。

禁止连续：

“Indoor or outdoor?”
“LED or LCD?”
“What size?”
“What viewing distance?”

应该：

先接住客户的话
↓
简短回应客户已经说的信息
↓
只解决当前最关键的产品类型问题
↓
自然进入下一步

例如客户：

“I need a display for my showroom.”

不要：

“What size?”
“What viewing distance?”
“What type?”

应该类似：

“Sure, I can help with that. For a showroom, we can look at either LED or LCD depending on the size and viewing experience you want. Do you already have a preference, or would you like me to recommend one based on your setup?”

要求：

1. 接住客户需求。
2. 不重复客户已经说的信息。
3. 不连续问多个问题。
4. 给客户选择。
5. 客户不知道时，AI 主动帮助判断。


十一、第九阶段：禁止 LLM 为了自然而编造信息

自然表达不能建立在虚构事实上。

例如客户只说：

“I need a display.”

不能说：

“Since your showroom needs a high-end display...”

因为客户没有提供 showroom。

正确：

“Sure, I can help with that. Do you already have a preference for LED or LCD, or would you like me to help you choose?”

原则：

自然 ≠ 编造。

自然语言只能包装已经确认的事实。


十二、第十阶段：产品类型状态管理

建议 Product Type 使用：

display_type
display_type_status
display_type_source
display_type_confidence
display_type_reason
display_type_locked

状态：

UNKNOWN
INFERRED
CONFIRMED

来源：

CUSTOMER
VISION
INFERENCE
DEFAULT

例如：

display_type = LED
status = INFERRED
source = INFERENCE
confidence = 0.86
locked = false

客户确认后：

display_type = LED
status = CONFIRMED
source = CUSTOMER
locked = true


十三、第十一阶段：产品类型锁定

客户明确：

“I want LED.”

则：

LED
CONFIRMED
CUSTOMER
LOCKED

后续普通信息：

meeting room
indoor
large screen

不能把 LED 自动改成 LCD。

只有客户明确修改：

“Actually, I want LCD.”

才允许：

LED
↓
LCD

然后重新进入 LCD 链路。


十四、第十二阶段：LED 链路保持不动

目前 LED 是项目中最成熟的需求链路。

本次改造不要重写：

Requirement Extraction
RequirementProfile
Recommendation Ready Gate
Hard Filter
Soft Score
Pitch
Brightness
Environment
Installation
Viewing Distance
Size
Recommendation Engine

最终：

Product Type Router
↓
LED
↓
现有成熟 LED 链路

Product Type Router 不负责具体 LED 型号选择。


十五、第十三阶段：LCD 链路重新建立

新的 LCD 链路不要直接继承旧链路全部逻辑。

先只建立：

LCD
↓
LCD Requirement Chain
↓
【具体需求链暂留白】
↓
LCD Recommendation

后续再独立设计：

LCD 场景
LCD 尺寸
LCD 拼接
LCD 拼接缝
LCD 安装
LCD 使用环境
LCD 观看距离
LCD 产品选择


十六、第十四阶段：IFP 作为 LCD 子类型

LCD 链路内部判断：

是否存在：

手写
触控
交互
电子白板
Interactive
Touch
Whiteboard
会议交互

等需求。

存在：

LCD
↓
IFP

不存在：

LCD
↓
普通 LCD

不要：

Product Router
↓
IFP


十七、第十五阶段：LCD/IFP 数据库

继续保持三个独立数据集：

data/led_products.json
data/lcd_products.json
data/ifp_products.json

关系：

LED
→ led_products.json

LCD
→ lcd_products.json

LCD + IFP
→ ifp_products.json

不需要合并 JSON。

数据库只提供产品数据。

需求判断决定产品类型。


十八、第十六阶段：旧 LCD/IFP 链路处理

不建议直接删除。

第一步：

完整梳理旧代码。

第二步：

从主流程隔离。

第三步：

标记 legacy。

第四步：

新 Product Type Router 不允许调用旧 LCD/IFP chain。

第五步：

新 LCD 链路独立开发。

第六步：

运行测试。

第七步：

新链路稳定以后删除旧代码。

最终保证：

旧 LCD/IFP 链路和新 LCD/IFP 链路不能同时参与最终决策。


十九、第十七阶段：多消息聚合

继续使用项目现有：

Session Lock
Turn Store
Message Dedup
Input Aggregation

例如客户连续发送：

I need a display.
For a showroom.
Indoor.
About 5m × 3m.

系统一次聚合：

scene = showroom
environment = indoor
size = 5m × 3m
display_type = UNKNOWN

然后一次执行 Product Type Router。

不能每条消息分别触发一次判断。


二十、第十八阶段：测试场景

至少建立以下测试：

1.
“I need an LED display.”
→ LED

2.
“I need an LCD display.”
→ LCD

3.
“I need an interactive display.”
→ LCD → IFP

4.
“I need a touch screen for meetings.”
→ LCD → IFP

5.
“I need a display.”
→ UNKNOWN
→ 自然询问

6.
“I need a display for outdoor advertising.”
→ LED

7.
“I need a screen for a meeting room.”
→ 分析用途
→ AI辅助判断
→ 向客户确认

8.
“I need a screen for a meeting room and we need to write on it.”
→ LCD → IFP

9.
“I need a huge display for a meeting room, viewing distance is 10m.”
→ 根据需求判断
→ AI给出倾向
→ 向客户确认

10.
客户发送 LED 图片
→ Vision
→ LED
→ 确认

11.
客户发送 LCD 图片
→ Vision
→ LCD
→ 确认

12.
图片无法判断
→ UNKNOWN
→ 询问

13.
AI 判断 LED
客户：“I don't know.”
→ 使用 AI 判断
→ LED

14.
客户不知道 LED/LCD
→ 简单解释
→ 再询问

15.
客户不知道选择什么
→ AI 根据用途辅助选型
→ 给出建议
→ 客户确认

16.
客户明确 LED
后续说 meeting room
→ 仍然 LED

17.
客户明确 LED
后面明确说：
“Actually, I want LCD.”
→ LED → LCD


二十一、最终核心决策规则

客户明确 LED
→ LED

客户明确 LCD
→ LCD

客户明确 IFP / 触控 / 手写 / 交互
→ LCD
→ 后续判断 IFP

客户发送图片
→ Vision 判断
→ 向客户确认

客户没说类型，但是知道用途
→ AI 根据用途和需求辅助判断
→ 给出建议
→ 向客户确认

客户不知道 LED/LCD
→ 简单解释
→ 再询问

客户完全不知道应该选什么
→ AI 帮助选型
→ 给出建议
→ 客户确认

无法判断
→ 询问 LED/LCD

所有方法都无法获得有效信息
→ 最终 fallback：LED

客户确认
→ 锁定类型

客户明确修改
→ 允许重新路由。


二十二、最终工程目标

最终系统：

First Contact
↓
Requirement Extraction
↓
Product Type Router
↓
┌─────────────────────────┐
│                         │
LED                       LCD
│                         │
▼                         ▼
现有成熟LED链              新LCD链
                            │
                            ├── 普通LCD
                            │
                            └── IFP


核心原则：

1. LED 现有成熟链路不动。
2. 第一层只负责 LED/LCD 分流。
3. IFP 属于 LCD。
4. LCD/IFP 数据库继续分开。
5. 图片由 Vision 辅助判断。
6. 客户不知道 LED/LCD 时先解释。
7. 客户只知道用途、不知道怎么选时，AI 主动帮助选型。
8. AI 推断需要客户确认，除非客户明确表示不知道并接受 AI 判断。
9. 最终无法判断才默认 LED。
10. 已确认类型必须锁定。
11. 客户明确修改时允许切换。
12. 旧 LCD/IFP 链路先隔离，再删除。
13. 新旧链路不能同时参与决策。
14. 多条客户消息必须聚合后一次处理。
15. AI 不能为了自然而编造客户没有提供的信息。
16. 对话必须“先接住客户，再判断，再自然推进”。
17. 避免连续提问和问卷式话术。
18. Product Type Router 只决定进入哪条链路，不负责具体产品型号推荐。

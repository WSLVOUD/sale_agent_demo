# LED RAG 智谱视觉需求提取接入实施计划

> 版本：v1.0  
> 日期：2026-09-16  
> 项目：`WSLVOUD/sale_agent_demo`  
> 目标：在现有 Sales Agent / RequirementProfile / Solution Agent 链路中接入智谱视觉大模型，使客户发送图片时，系统能够提取图片中可识别的 LED/显示屏需求，并将结果合并到现有需求状态，再由语言模型根据缺失需求继续推进销售工作流。

---

## 执行进度总览（更新时间：2026-09-16）

| 计划章节 | 阶段 | 内容 | 状态 |
| --- | --- | --- | --- |
| §3 | 第一阶段 | 确认现有代码接口 | ✅ 已完成 |
| §4 | 第二阶段 | 新增 `src/vision/` 模块 | ✅ 已完成 |
| §5 | 第三阶段 | 设计 Vision Schema（含来源） | ✅ 已完成 |
| §6 | 第四阶段 | 严格区分"看见"和"猜测" | ✅ 已完成 |
| §7 | 第五阶段 | 设计 Vision Prompt | ✅ 已完成 |
| §8 | 第六阶段 | 实现 Vision Extractor（含单位标准化） | ✅ 已完成 |
| §9 | 第七阶段 | 接入 RequirementProfile | ✅ 已完成 |
| §10 | 第八阶段 | 建立字段优先级 | ✅ 已完成 |
| §11 | 第九阶段 | 处理冲突 | ✅ 已完成 |
| §12 | 第十阶段 | 图片识别后重新计算 Missing Slots | ✅ 已完成 |
| §13 | 第十一阶段 | 解决"答非所问" | ✅ 已完成 |
| §14 | 第十二阶段 | 图片 + 文字同时发送 | ✅ 已完成 |
| §15 | 第十三阶段 | 修改 API 输入（支持 images） | ✅ 已完成 |
| §16 | 第十四阶段 | Orchestrator 接入 | ✅ 已完成 |
| §17 | 第十五阶段 | First Contact 兼容 | ✅ 已完成 |
| §18 | 第十六阶段 | 继续使用现有 Question Planner | ✅ 已完成 |
| §19 | 第十七阶段 | Gate 不因"有图片"直接放行 | ✅ 已完成 |
| §20 | 第十八阶段 | Calculator 不接受视觉猜测尺寸 | ✅ 已完成 |
| §21 | 第十九阶段 | 异常处理（失败降级） | ✅ 已完成 |
| §22 | 第二十阶段 | 性能控制（图片哈希缓存） | ✅ 已完成 |
| §23 | 第二十一阶段 | 安全控制（MIME / 大小 / 数量 / 超时） | ✅ 已完成 |
| §24 | 第二十二阶段 | 日志与 Observability | ✅ 已完成 |
| §25 | 第二十三阶段 | Vision Golden Dataset | ✅ 已完成（合成基线，待补真实图片） |
| §26 | — | Vision 测试指标 | ✅ 已完成 |
| §27 | 第二十四阶段 | 端到端测试（Case 1~7） | ✅ 已完成 |
| §28 | 第二十五阶段 | 性能目标 | ✅ 已完成（见附录 B） |
| §34 | — | 第一版完成标准（17 项） | ✅ 全部满足 |

> **接口实测**：已用 `.env` 里的 `GLM_API_KEY` 真实调用智谱视觉接口（模型 `glm-4v-plus`）成功。
> **Golden 基线**（8 张合成图）：display_type 100%、environment 87.5%、purpose 100%、
> installation 100%、null_precision 100%（没屏幕就不填）、明确尺寸读取 100%、
> 尺寸编造率 0%、视觉推测被当成确认 0。
> **回归测试**：`python -m pytest tests -q` → **834 passed, 4 skipped**。

---

## 1. 项目目标

### 1.1 核心目标

实现以下完整链路：

```text
客户发送图片
    ↓
API / Orchestrator 接收图片
    ↓
智谱视觉大模型
    ↓
视觉需求结构化提取
    ↓
Vision Extraction Result
    ↓
合并到 RequirementProfile
    ↓
重新计算已知需求 / 缺失需求 / 冲突
    ↓
Sales Agent / Question Planner
    ↓
只询问仍然缺失的关键需求
    ↓
客户继续回复
    ↓
RequirementProfile 持续更新
    ↓
Recommendation Ready Gate
    ↓
Solution Agent
    ↓
产品推荐 / RAG / 屏幕计算
```

### 1.2 非目标

第一版视觉模块**不负责**：

- 产品推荐
- 产品排序
- 价格判断
- 产品对比
- 模块/箱体最终计算
- 替代 Sales Agent
- 替代 Solution Agent
- OCR 全量提取图片文字
- 根据图片猜测结果直接作为客户确认事实

视觉模型只负责：

> **从客户图片中提取与 LED / 显示产品需求有关的信息。**

---

# 2. 现有项目适配原则

本次改造以当前仓库实际架构为基础，不重构现有 Agent。

现有核心链路保持：

```text
Orchestrator
    ↓
Sales Agent
    ↓
RequirementProfile
    ↓
Recommendation Gate
    ↓
Solution Agent
    ↓
RAG / Recommendation / Calculator
```

新增视觉能力后：

```text
                     ┌── Text Requirement Extraction
                     │
Customer Input ──────┤
                     │
                     └── Image → Zhipu Vision
                                  ↓
                           Vision Requirement
                                  ↓
                         RequirementProfile
                                  ↓
                            Sales Agent
```

原则：

1. **RequirementProfile 继续作为唯一统一需求状态。**
2. 不新增一套独立的 `image_requirements` 长期状态。
3. Vision 结果作为 RequirementProfile 的一种数据来源。
4. 视觉模型不直接决定下一步销售动作。
5. 销售语言模型不重新解释整张图片，而是读取已经合并后的需求状态。
6. Solution Agent 继续负责推荐。
7. Calculator 继续负责工程计算。

---

# 3. 第一阶段：确认现有代码接口
> ✅ **已完成（2026-09-16）** —— 已核对真实调用链，与 README 描述一致：
> `POST /chat`（`ChatRequest{session_id, question}`）→ `DualAgentOrchestrator.process_message`
> → `SalesAgentRunner.run(session_id, message)`（从 `MemoryStore` 读 `RequirementProfile`）
> → `requirement_mining`（唯一 Gate：`src/rag/readiness.py`）→ `script_generator`
> → 就绪时 `Orchestrator` 调 `SolutionAgentRunner.run(...)`。
> 现状：**图片输入接口字段不存在**（本轮新增）。已有 `last_asked_slot` / `ask_counts` /
> `conflicts` / `sources` 可直接承载视觉结果。

## 3.1 需要重点核对的现有文件

实施前先确认以下文件当前版本：

```text
src/orchestrator.py

src/api.py

src/agents/sales/graph.py
src/agents/sales/runner.py
src/agents/sales/state.py

src/agents/sales/nodes/classify.py
src/agents/sales/nodes/requirement.py
src/agents/sales/nodes/router.py
src/agents/sales/nodes/script_generator.py

src/agents/sales/question_planner.py

src/models/requirement.py

src/agents/solution/*
src/rag/*
src/tools/*
src/memory/*
```

重点确认：

- 当前请求对象如何接收文本；
- Session ID 如何传递；
- RequirementProfile 如何创建；
- RequirementProfile 如何更新；
- Sales Agent 如何读取 RequirementProfile；
- 缺失需求如何计算；
- `last_asked_slot` 如何使用；
- Recommendation Ready Gate 在哪里判断；
- Solution Agent 的触发条件；
- 图片输入目前是否已经存在接口字段。

## 3.2 阶段验收

完成后必须能够画出真实代码调用链：

```text
API
 ↓
Orchestrator
 ↓
Sales
 ↓
RequirementProfile
 ↓
Question Planner
 ↓
Solution
```

不得根据 README 猜测调用关系。

---

# 4. 第二阶段：新增 Vision 模块
> ✅ **已完成（2026-09-16）** —— 新增 `src/vision/`：
> `client.py`（只调智谱）、`extractor.py`（解析+标准化+缓存）、`schema.py`（结构）、
> `prompts.py`（提示词）、`integration.py`（合并进 RequirementProfile）。
> 职责边界严格：视觉模块不推荐、不选型、不追问、不算箱体。
> 验收：`tests/test_vision.py`（50 个用例）。

建议新增：

```text
src/
└── vision/
    ├── __init__.py
    ├── client.py
    ├── extractor.py
    ├── schema.py
    └── prompts.py
```

---

## 4.1 `src/vision/client.py`

职责：

> 只负责调用智谱视觉大模型。

输入：

```text
image
```

输出：

```text
raw vision response
```

不得在这里处理：

- RequirementProfile
- Sales Agent
- 推荐
- 追问
- RAG

建议接口：

```python
class ZhipuVisionClient:
    async def analyze_image(
        self,
        image,
        prompt: str,
    ) -> str:
        ...
```

具体模型名称、API Endpoint、认证方式以当前智谱官方接口文档和项目实际账号配置为准，不在业务代码中硬编码。

---

# 5. 第三阶段：设计 Vision Schema
> ✅ **已完成（2026-09-16）** —— `src/vision/schema.py`：
> `VisionField{value, confidence, source, evidence}` + `VisionRequirement`。
> 核心字段与计划 5.1 一致；可扩展字段（screen_shape / cabinet_type_hint / technology_hint /
> rental_hint / curved_hint / transparent_hint）已预留但**不参与决策**。
> 来源：`vision_explicit` / `vision_inferred`。

## 5.1 核心字段

第一版只提取与当前 LED 销售流程直接相关的字段：

```text
display_type
environment
purpose
installation
target_width_m
target_height_m
viewing_distance_m
pixel_pitch_mm
brightness_min_nit
brightness_max_nit
special_requirements
```

可扩展字段：

```text
screen_shape
cabinet_type_hint
technology_hint
rental_hint
curved_hint
transparent_hint
```

但第一版不要过度扩展。

---

## 5.2 每个字段必须带来源

推荐结构：

```json
{
  "environment": {
    "value": "indoor",
    "confidence": 0.96,
    "source": "vision_explicit"
  }
}
```

来源至少定义：

```text
vision_explicit
vision_inferred
```

含义：

### `vision_explicit`

图片能够比较明确地支持该信息。

例如：

```text
明显室内会议室
明显 LED 屏
明显租赁箱体
```

### `vision_inferred`

模型根据图片进行推断。

例如：

```text
可能用于会议室
可能为固定安装
大概尺寸
```

---

# 6. 第四阶段：严格区分“看见”和“猜测”
> ✅ **已完成（2026-09-16）** —— 三道防线：
> 1. Prompt 强制"看不出来返回 null，禁止编造"；
> 2. 模型没按格式返回（裸值 / 来源写错）时**一律按 vision_inferred 处理**；
> 3. `vision_inferred` 不进 Gate 的"已确认"集合，`vision_explicit` 只对
>    display_type / environment / purpose / installation 生效（图片确实能看出来的项）。
> 验收：`TestSchemaCoercion`、`TestGateWithVision::test_viewing_distance_from_image_still_asked`。

这是整个功能最重要的工程规则。

## 6.1 图片明确出现

例如：

```text
会议室
LED display
Indoor
```

可以：

```text
purpose = conference
environment = indoor
display_type = LED
```

并记录：

```text
source = vision_explicit
```

---

## 6.2 图片无法确认

必须：

```json
{
  "target_width_m": null,
  "target_height_m": null,
  "viewing_distance_m": null,
  "pixel_pitch_mm": null
}
```

禁止视觉模型为了让 JSON 完整而编造数字。

---

## 6.3 图片只能估计

例如模型认为：

```text
屏幕大约 5m × 3m
```

不能直接：

```text
target_width_m = 5
target_height_m = 3
```

而应该：

```text
vision_inferred
```

保存为提示性信息，不能直接满足“客户确认尺寸”。

---

# 7. 第五阶段：设计 Vision Prompt
> ✅ **已完成（2026-09-16）** —— `src/vision/prompts.py`：只提取需求、不销售、不推荐、
> 不选 P 值、不算箱体模组、不输出话术；明确要求 null 语义与 source 标注；只返回 JSON。

新增：

```text
src/vision/prompts.py
```

Prompt 核心要求：

```text
你是 LED 显示屏销售系统的视觉需求提取器。

你的任务不是销售，不是推荐产品，也不是回答客户。

只从图片中提取能够帮助 LED 产品销售流程判断客户需求的信息。

只提取以下字段：
- display_type
- environment
- purpose
- installation
- target_width_m
- target_height_m
- viewing_distance_m
- pixel_pitch_mm
- brightness_min_nit
- brightness_max_nit
- special_requirements

如果图片无法确认，返回 null。

如果只是推测，必须标记 source=vision_inferred。

不要把推测当成客户确认需求。

不要推荐产品。

不要选择 P 值。

不要计算箱体数量。

不要计算模块数量。

不要输出销售话术。

最终严格返回 JSON。
```

---

# 8. 第六阶段：实现 Vision Extractor
> ✅ **已完成（2026-09-16）** —— `VisionExtractor`：
> ```json 代码块 / 前后废话 / 尾逗号 都能安全解析；坏 JSON 抛 `VisionError`（调用方降级）；
> 枚举与数值范围校验（无效字段直接丢弃，绝不猜）。
> 单位标准化：`5000 mm` / `5 m` / `500 cm` / `16 ft` / `200 inch` / `5米` → 米；
> 点间距 → 毫米（0.3~20mm 之外判为编造）。
> 验收：`TestParseResponse`、`TestUnitNormalization`。

新增：

```text
src/vision/extractor.py
```

职责：

```text
智谱原始响应
      ↓
JSON 提取
      ↓
Schema 校验
      ↓
字段标准化
      ↓
单位标准化
      ↓
VisionRequirement
```

需要处理：

### JSON 异常

模型可能返回：

```text
```json
{...}
```
```

或者：

```text
Here is the result:
{...}
```

Extractor 必须能够安全处理。

---

## 8.1 单位标准化

例如：

```text
5000 mm
5 m
16 ft
```

统一转换为：

```text
target_width_m
target_height_m
viewing_distance_m
```

但是：

> 只有能够可靠解析的数据才能进入结构化字段。

---

# 9. 第七阶段：接入 RequirementProfile
> ✅ **已完成（2026-09-16）** —— `src/vision/integration.py::apply_vision_to_profile()`
> 把视觉结果并进**现有** `RequirementProfile`（没有 image_requirement_profile），
> 并逐字段写入 `sources`。Orchestrator 在 Sales Agent 之前合并并写回 memory，
> Sales 从 memory 读到的就是已经合并好的档案。
> 验收：`TestApplyVisionToProfile`、`TestImageOnlyTurn`。

这是本次改造的核心。

新增：

```text
VisionResult
    ↓
RequirementProfile.merge(...)
```

不要创建：

```text
image_requirement_profile
```

而是：

```text
现有 RequirementProfile
+
Vision Result
=
新的 RequirementProfile
```

---

# 10. 第八阶段：建立字段优先级
> ✅ **已完成（2026-09-16）** —— `_EXPLICIT_STRENGTH` 七级：
> `explicit/confirmed(7) > vision_explicit(5) > scenario_derived(4) > vision_inferred(3)
> > inferred(2) > default(1)`。客户的明确说法永远不会被图片覆盖。
> 验收：`test_customer_value_is_never_overwritten`、`test_vision_inferred_does_not_beat_explicit`。

建议字段来源优先级：

```text
customer_explicit
        ↓
customer_confirmed
        ↓
vision_explicit
        ↓
scenario_derived
        ↓
vision_inferred
        ↓
inferred
        ↓
default
```

因此：

```text
客户明确说 outdoor
```

优先于：

```text
图片模型推断 indoor
```

---

# 11. 第九阶段：处理冲突
> ✅ **已完成（2026-09-16）** —— 客户值保留，冲突写入 `profile.conflicts` 与
> `profile.conflict_slots`；Gate 检测到冲突时不放行，并且**就问冲突的那一项**
> （图片像室内、客户说室外 → 直接问 indoor/outdoor），而不是笼统问场景。
> 验收：`TestConflictBetweenCustomerAndImage`。

例如：

客户文字：

```text
I need an outdoor display.
```

RequirementProfile：

```text
environment = outdoor
source = customer_explicit
```

图片模型：

```text
environment = indoor
source = vision_inferred
```

不能直接覆盖。

应该：

```text
environment = outdoor

conflicts:
- customer explicitly specified outdoor
- vision inferred indoor from image
```

然后由 Sales Agent 询问：

```text
Is the screen intended for outdoor use? The image appears to show an indoor installation.
```

---

# 12. 第十阶段：图片识别后重新计算 Missing Slots
> ✅ **已完成（2026-09-16）** —— 图片不重启流程：合并后照常走
> `RequirementProfile → Missing Slots → Question Planner`，只问仍然缺的项；
> 图片里明确可见的项不会再问一遍。
> 验收：`TestGateWithVision::test_clearly_visible_fields_are_not_asked_again`。

图片识别结束后，不要重新开始销售流程。

应该：

```text
RequirementProfile
       ↓
Missing Slots
       ↓
Question Planner
```

例如：

图片识别：

```text
display_type = LED
environment = indoor
purpose = conference
installation = fixed
```

仍缺：

```text
target_width_m
target_height_m
viewing_distance_m
```

那么 Sales Agent 继续询问缺失项。

---

# 13. 第十一阶段：解决“答非所问”
> ✅ **已完成（2026-09-16）** —— 沿用统一的 `RequirementExtractor` 全量提取（见
> 《全量需求捕获与 Unknown 容错优化》），客户答非所问时其它字段照常入库；
> 图片同理：一次可能同时给出 environment + purpose + installation，全部保存，
> 与当前正在问的问题是哪一项无关。

必须保持现有项目的重要原则：

> AI 当前问什么，不代表客户下一句话只能回答这个字段。

例如：

AI：

```text
What screen size are you looking for?
```

客户：

```text
Viewing distance is about 8 meters.
```

系统必须：

```text
识别 viewing_distance_m = 8
        ↓
更新 RequirementProfile
        ↓
target_width_m / target_height_m 仍然缺失
        ↓
继续询问尺寸
```

同样适用于图片。

图片中可能同时识别出：

```text
environment
purpose
installation
```

即使当前 Sales Agent 正在问：

```text
budget
```

这些视觉结果也必须全部保存。

---

# 14. 第十二阶段：图片 + 文字同时发送
> ✅ **已完成（2026-09-16）** —— 文字走 Text Extractor、图片走 Vision，
> 两者都合并进**同一个** RequirementProfile（先合并图片、再合并本轮文字）。
> 纯图片消息（没有文字）也支持：会用一句"照片已收到"驱动需求采集，
> 而不是拿空字符串走自由问答。
> 验收：`TestImageOnlyTurn::test_image_and_text_share_one_profile`。

必须支持：

```text
文字 + 图片
```

例如：

```text
Can you recommend something like this?
[image]
```

处理顺序：

```text
Text
 ↓
Text Requirement Extraction

Image
 ↓
Zhipu Vision

两个结果
 ↓
统一 Merge
 ↓
RequirementProfile
```

最终：

```text
RequirementProfile
```

只能有一个。

---

# 15. 第十三阶段：修改 API 输入
> ✅ **已完成（2026-09-16）** —— `ChatRequest` 扩展为
> `{session_id, question, images:[{url|data, mime_type}]}`；`question` 允许为空
> （纯图片消息）；旧的纯文字请求完全兼容。响应新增 `vision` 指标字段。
> 前端 `static/` 也加了"粘贴 / 上传图片 + 预览"，可直接在页面测试。
> 验收：`TestApiImagePayload`。

当前 API 如果只有：

```json
{
  "session_id": "...",
  "message": "..."
}
```

需要扩展成支持图片。

推荐：

```json
{
  "session_id": "...",
  "message": "Can you recommend something like this?",
  "images": [
    {
      "url": "...",
      "mime_type": "image/jpeg"
    }
  ]
}
```

如果当前入口已经是文件上传，则根据实际 API 保留现有协议，不要为了 Vision 重构整个接口。

---

# 16. 第十四阶段：Orchestrator 接入
> ✅ **已完成（2026-09-16）** —— Orchestrator 只做协调：
> 有图片 → `extract_vision_for_turn()`（失败自动降级）→
> `_merge_vision_into_stored_profile()` 写回 memory → 原来的 Sales/Solution 流程。
> 视觉业务逻辑都在 `src/vision/`，Orchestrator 里没有第二套实现。

Orchestrator 负责：

```text
判断是否存在图片
```

如果没有：

```text
原有流程
```

如果有：

```text
图片
 ↓
Vision Extractor
 ↓
RequirementProfile Merge
 ↓
进入现有 Sales Agent
```

重要：

> 不要在 Orchestrator 中写完整视觉业务逻辑。

Orchestrator 只负责协调。

---

# 17. 第十五阶段：First Contact 兼容
> ✅ **已完成（2026-09-16）** —— 首次消息带图片时：仍然完整执行 First Contact
> （照常发自我介绍/视频/名片，不跑 Sales Agent），但图片识别出的需求会一并保存到
> 需求档案，下一轮追问时不再当"没说过"。
> 验收：`TestFirstContactWithImage`。

如果客户第一次消息就是：

```text
图片
```

流程应该：

```text
First Contact
    ↓
图片 Vision
    ↓
保存需求
    ↓
First Contact 流程
    ↓
后续 Sales Agent
```

不能因为图片消息而绕过 First Contact。

如果当前 First Contact 设计要求先执行固定接待流程，则保持现有规则。

---

# 18. 第十六阶段：Sales Agent 继续使用现有 Question Planner
> ✅ **已完成（2026-09-16）** —— 没有新增 Vision Sales Agent。
> Sales Agent 看到的仍然是 RequirementProfile（已知 / 缺失 / 推测 / 冲突），
> 不读视觉原始输出。

视觉模块完成后，不创建：

```text
Vision Sales Agent
```

而是：

```text
RequirementProfile
        ↓
Question Planner
        ↓
现有 Sales Agent
```

Sales Agent 最终看到的是：

```text
当前客户已经知道什么
+
还缺什么
+
哪些信息是推测
+
哪些信息存在冲突
```

而不是直接读取 Vision 原始文本。

---

# 19. 第十七阶段：推荐 Gate 不因“图片存在”直接放行
> ✅ **已完成（2026-09-16）** —— `has_image` 与 `recommendation_ready` 完全无关：
> 图片只填字段，放行判断仍在 `check_recommendation_ready()`。
> 实测：图片给出 LED + indoor + conference 后，安装方式与观看距离仍然会继续问，
> `ready=False`。
> 验收：`TestGateWithVision::test_image_alone_is_not_recommendation_ready`。

非常重要：

```text
has_image = True
```

不能代表：

```text
recommendation_ready = True
```

必须继续判断：

```text
Recommendation Ready Gate
```

例如：

```text
图片：
✓ LED
✓ indoor
✓ conference

缺失：
✗ size
✗ viewing distance
```

不能直接推荐。

应该继续追问。

---

# 20. 第十八阶段：Calculator 不接受视觉猜测作为最终尺寸
> ✅ **已完成（2026-09-16）** —— **结构性保证**：视觉给的宽高一律写入
> `profile.vision_size_hint_mm`，**从不**写进 `target_width_m / target_height_m`，
> 因此 Calculation Gate 与箱体/模组计算在代码路径上不可能用到图片尺寸。
> 图片有尺寸时，追问会带上"图片上看大约是 5 米 × 3 米"，让客户确认。
> 验收：`test_vision_size_only_becomes_hint`、`test_vision_size_does_not_open_calculation_gate`、
> `test_size_question_mentions_image_hint`。

屏幕计算仍然要求可靠尺寸：

```text
target_width_m
target_height_m
```

只有：

```text
customer_explicit
customer_confirmed
```

或者明确允许的高可信来源，才能进入最终工程计算。

视觉估计：

```text
5m × 3m
```

只能作为：

```text
size_hint
```

不能直接进入：

```text
cabinet_count
module_count
```

---

# 21. 第十九阶段：异常处理
> ✅ **已完成（2026-09-16）** —— 超时（重试后抛 `VisionError(timeout)`）、
> 5xx（重试）、JSON 解析失败、格式错误、MIME 不支持、图片过大、Key 未配置
> 全部被捕获：记录日志 → RequirementProfile 保持原状态 → Sales Agent 照常工作。
> 验收：`TestVisionFailSafe`、`TestVisionFailureIsSafe`。

Vision API 失败时：

```text
Vision API timeout
Vision API 5xx
JSON parse error
模型输出格式错误
图片格式不支持
图片过大
```

都不能让整个 Sales Agent 崩溃。

必须降级：

```text
Vision Failure
    ↓
记录日志
    ↓
RequirementProfile 保持原状态
    ↓
Sales Agent 正常继续
```

例如：

```text
I can help you choose the right LED display. Could you tell me whether it will be used indoors or outdoors?
```

---

# 22. 第二十阶段：性能控制
> ✅ **已完成（2026-09-16）** —— 图片 → SHA-256 → `(session_id, image_hash)` 缓存：
> 同一会话同一张图只调一次；会话之间不共享（避免跨会话串味）。
> 多图一次最多 `VISION_MAX_IMAGES`（默认 3）张。
> 验收：`TestExtractorCache`。

图片识别会增加一次视觉模型调用，因此必须控制调用次数。

推荐：

```text
同一张图片
+
同一 session
+
同一 image hash
```

避免重复调用。

增加：

```text
image_hash
vision_result_cache
```

例如：

```text
image
 ↓
SHA-256
 ↓
cache lookup
 ↓
存在 → 直接使用
不存在 → 调用智谱
```

---

# 23. 第二十一阶段：安全控制
> ✅ **已完成（2026-09-16）** —— MIME 白名单（jpeg/png/webp/gif/bmp）、
> 单张大小上限（`VISION_MAX_IMAGE_MB`，默认 5MB）、单次数量上限
> （`VISION_MAX_IMAGES`，默认 3）、超时控制（`VISION_TIMEOUT_SECS`）、异常捕获；
> URL 与 data URL 都支持（URL 由模型服务端拉取）。
> 验收：`TestImagePayloadChecks`。

图片进入模型前进行：

- MIME 类型检查
- 文件大小限制
- 图片数量限制
- URL 白名单/安全下载策略（如果入口是 URL）
- 超时控制
- 异常捕获

第一版建议：

```text
单次最多 3 张图片
单张限制大小
只允许常见图片格式
```

具体数值根据现有入口和智谱接口限制配置化。

---

# 24. 第二十二阶段：日志与 Observability
> ✅ **已完成（2026-09-16）** —— 每轮记录（不记录图片内容）：
> `images / vision_latency_ms / vision_success / fields_extracted / fields_inferred /
> fields_null / special_requirements / parse_success / conflict_count / image_hash / model`，
> 并通过 `POST /chat` 的响应 `vision` 字段返回，便于以后分析"视觉到底有没有帮助"。

不要记录完整图片内容。

建议记录：

```text
session_id
image_hash
vision_model
vision_latency_ms
vision_success
fields_extracted
fields_inferred
fields_null
parse_success
merge_success
conflict_count
```

例如：

```json
{
  "vision_success": true,
  "vision_latency_ms": 1240,
  "fields_extracted": 4,
  "fields_inferred": 1,
  "fields_null": 6,
  "conflict_count": 0
}
```

这样以后可以分析：

> 智谱视觉模型到底有没有真正帮助需求采集。

---

# 25. 第二十三阶段：测试体系
> ✅ **已完成（2026-09-16，第一版）** —— `tests/vision_golden/`：
> `generate_images.py`（生成图片 + `manifest.json`）、`images/`（8 张）、
> `run_golden.py`（跑 Golden 并输出 `last_report.json`）。
> 分类覆盖：室内固定（2）、户外（2）、租赁舞台（1）、带尺寸标注（1）、
> 无屏幕/无法判断（2）。
> ⚠️ **待补**：当前是**合成图**，用来回归链路与"是否会瞎填"；
> 计划要求的 50 张**脱敏真实客户图片**需要业务侧提供后替换，才能得到有意义的精度基线。

必须建立 Vision Golden Dataset。

建议第一版准备：

```text
50 张图片
```

分类：

```text
10 张室内固定 LED
10 张户外 LED
10 张租赁 LED
5 张会议室
5 张商场/广告
5 张舞台
5 张模糊/无法判断
```

如果有真实历史客户图片，优先使用经过脱敏处理的真实图片。

---

# 26. Vision 测试指标
> ✅ **已完成（2026-09-16）** —— `run_golden.py` 输出：
> `display_type_accuracy / environment_accuracy / purpose_accuracy /
> installation_accuracy / null_precision / clear_size_accuracy /
> size_fabrication_rate / vision_inferred_as_confirmed`。
> 基线结果见附录 B。

至少统计：

### Display Type Accuracy

```text
正确识别 LED / LCD / IFP 等比例
```

### Environment Accuracy

```text
Indoor / Outdoor
```

### Purpose Accuracy

```text
Conference / Advertising / Rental / etc.
```

### Null Precision

重点测试：

> 模型是否会在看不出来时胡乱填值。

### Inference Precision

重点测试：

> vision_inferred 是否被错误当成客户确认需求。

### Profile Merge Accuracy

测试：

```text
图片结果
+
客户原有需求
```

能否正确合并。

### Conflict Detection

测试：

```text
客户文字
VS
图片视觉结果
```

是否正确产生冲突。

---

# 27. 第二十四阶段：端到端测试
> ✅ **已完成（2026-09-16）** —— Case 1~7 全部落地：`tests/test_vision_pipeline.py`（13 个用例）
> 覆盖纯图片 / 图文 / 冲突 / Vision 失败 / 首轮接待；Case 6（客户答非所问）由
> `tests/test_unknown_tolerance.py` 覆盖。

必须至少覆盖以下场景。

## Case 1：纯图片

```text
客户：发送图片
```

预期：

```text
Vision
 ↓
RequirementProfile
 ↓
Sales 继续追问
```

---

## Case 2：图片 + 文字

```text
客户：
I need something like this.
[image]
```

预期：

```text
文字 + 图片
 ↓
统一 RequirementProfile
```

---

## Case 3：图片已经包含大部分需求

```text
图片：
室内会议室 LED
明显固定安装
```

预期：

```text
只继续问真正缺失的信息
```

---

## Case 4：图片无法确定尺寸

预期：

```text
target_width = null
target_height = null
```

不能自行生成精确尺寸。

---

## Case 5：客户与图片冲突

```text
客户：Outdoor
图片：明显室内
```

预期：

```text
客户信息保留
产生 conflict
后续确认
```

---

## Case 6：客户答非所问

```text
AI：What screen size do you need?

客户：Viewing distance is 8 meters.
```

预期：

```text
viewing_distance = 8
size 继续缺失
继续询问 size
```

---

## Case 7：Vision API 失败

预期：

```text
Vision 失败
 ↓
系统不崩溃
 ↓
Sales Agent 正常工作
```

---

# 28. 第二十五阶段：性能目标
> ✅ **已完成（2026-09-16）** —— 见附录 B 的实测值：
> Vision API 成功率 100%（8/8）、JSON 解析成功率 100%、
> 尺寸编造率 0%、视觉推测被当成 confirmed = 0、
> Vision 失败导致主流程失败 = 0、同图重复调用 = 0。
> 环境识别 87.5% 未达 95%：唯一失败用例是"无文字标注的合成户外广告图"，
> 需要用真实照片复核后再定基线。

第一版建议设置：

| 指标 | 目标 |
|---|---:|
| Vision API 成功率 | ≥ 99% |
| JSON 解析成功率 | ≥ 99% |
| 核心字段提取准确率 | ≥ 90% |
| 环境识别准确率 | ≥ 95% |
| 明确尺寸误识别率 | ≤ 5% |
| Vision inferred 被当成 confirmed | 0 |
| Vision API 失败导致主流程失败 | 0 |
| 同图重复调用 | 0 |

实际目标值应在 Golden Dataset 跑完基线后调整。

---

# 29. 推荐最终代码架构

完成后的逻辑建议：

```text
src/
├── api.py
├── orchestrator.py
│
├── vision/
│   ├── __init__.py
│   ├── client.py
│   ├── extractor.py
│   ├── schema.py
│   └── prompts.py
│
├── agents/
│   ├── sales/
│   │   └── ...
│   └── solution/
│       └── ...
│
├── models/
│   └── requirement.py
│
├── rag/
│   └── ...
│
├── memory/
│   └── ...
│
└── observability/
    └── ...
```

---

# 30. 最终数据流

```text
                    Customer
                       │
              ┌────────┴────────┐
              │                 │
            Text              Image
              │                 │
              ↓                 ↓
       Text Extractor     Zhipu Vision
              │                 │
              │          Vision Extractor
              │                 │
              └────────┬────────┘
                       ↓
               RequirementProfile
                       │
              ┌────────┴────────┐
              │                 │
           Conflicts        Missing Slots
              │                 │
              └────────┬────────┘
                       ↓
                  Sales Agent
                       ↓
                 Question Planner
                       ↓
                  Customer Reply
                       ↓
               RequirementProfile
                       ↓
             Recommendation Gate
                       ↓
                Solution Agent
                       ↓
           ┌───────────┴───────────┐
           ↓                       ↓
          RAG                 Calculator
           ↓                       ↓
      Product Answer       Cabinet / Module
```

---

# 31. 实施顺序

不要一次修改整个项目。

严格按照下面顺序实施：

```text
Phase 1
读取并确认现有接口
        ↓
Phase 2
新增 src/vision/
        ↓
Phase 3
接通智谱 Vision API
        ↓
Phase 4
实现 JSON Schema
        ↓
Phase 5
实现 Vision Extractor
        ↓
Phase 6
单元测试 Vision
        ↓
Phase 7
接入 RequirementProfile
        ↓
Phase 8
实现冲突与来源优先级
        ↓
Phase 9
Orchestrator 接入
        ↓
Phase 10
图片 + 文字
        ↓
Phase 11
Sales Agent 缺失槽位继续追问
        ↓
Phase 12
Recommendation Gate 联调
        ↓
Phase 13
Solution Agent 联调
        ↓
Phase 14
异常 / 超时 / 缓存
        ↓
Phase 15
Golden Dataset
        ↓
Phase 16
端到端回归
```

---

# 32. 不建议做的事情

## 不要

```text
图片 → Vision Agent → 推荐产品
```

## 不要

```text
图片 → Vision → 直接覆盖 RequirementProfile
```

## 不要

```text
图片 → 猜尺寸 → Calculator
```

## 不要

```text
图片 → 新建一套独立销售流程
```

## 不要

```text
图片识别结果 → 直接作为客户确认事实
```

## 不要

```text
Vision 负责销售话术
```

---

# 33. 与未来 n8n 集成的兼容设计

未来如果将当前 LangGraph 系统作为 n8n 工作流中的 AI 能力，不需要推翻 Vision 模块。

建议最终对外暴露统一接口：

```text
POST /chat
```

输入：

```json
{
  "session_id": "...",
  "message": "...",
  "images": [...]
}
```

n8n：

```text
WhatsApp
   ↓
n8n Workflow
   ↓
你的 AI API
   ↓
Orchestrator
   ↓
Vision / Sales / Solution
   ↓
AI Response
   ↓
n8n
   ↓
WhatsApp
```

这样：

```text
n8n = 外部业务工作流
你的项目 = AI 需求理解 + RAG + 推荐 + 计算能力
```

两者职责清晰。

---

# 34. 第一版完成标准

只有满足以下条件，才认为 Vision MVP 完成：

- [x] 智谱视觉 API 能正常调用（`glm-4v-plus` 实调成功）
- [x] 图片能够转换成标准 Vision JSON
- [x] 模型无法确认的信息返回 null（Golden null_precision = 1.0）
- [x] Vision explicit / inferred 能区分（裸值/错误来源一律按 inferred）
- [x] Vision 结果能够合并到 RequirementProfile
- [x] 不破坏客户已经明确提供的信息
- [x] 能检测客户信息与图片信息冲突
- [x] 图片 + 文字能够同时处理
- [x] 图片识别后能够继续进入 Sales Agent
- [x] Sales Agent 能根据新的 Missing Slots 继续追问
- [x] 客户答非所问时，其他需求仍然能够被提取
- [x] Vision API 失败不会导致主流程崩溃
- [x] 不会因为“有图片”直接触发推荐
- [x] 不会把视觉猜测直接用于最终工程计算（图片尺寸只进 `vision_size_hint_mm`）
- [x] 有 Vision Golden Dataset（`tests/vision_golden/`；⚠️ 第一版为合成图，待补真实图片）
- [x] 有端到端回归测试（`tests/test_vision_pipeline.py` Case 1~7）
- [x] 有 Vision latency / success / extraction metrics

---

# 35. 最终设计结论

本次功能不是给现有项目“再加一个 Agent”。

而是增加一个：

> **Zhipu Vision Requirement Extractor**

它的唯一核心职责是：

```text
客户图片
   ↓
智谱视觉模型
   ↓
识别 LED / Display 相关需求
   ↓
结构化
   ↓
合并 RequirementProfile
```

然后继续复用当前项目：

```text
RequirementProfile
        ↓
Sales Agent
        ↓
Question Planner
        ↓
Recommendation Gate
        ↓
Solution Agent
        ↓
RAG / Recommendation / Calculator
```

这样能够最大程度复用当前已经完成的 LangGraph、需求状态、RAG、推荐和计算能力，同时为以后接入 n8n、WhatsApp 等外部工作流保留清晰的 API 边界。

---

# 附录 A：改动文件清单（2026-09-16）

## 新增

| 文件 | 作用 |
| --- | --- |
| `src/vision/__init__.py` | 模块出口（client / extractor / integration） |
| `src/vision/client.py` | 智谱视觉客户端：只调模型；MIME/大小检查、超时、重试、模型回退 |
| `src/vision/prompts.py` | 视觉提示词（只提取需求、禁止编造、必须标 source） |
| `src/vision/schema.py` | `VisionField` / `VisionRequirement`（value + confidence + source + evidence） |
| `src/vision/extractor.py` | JSON 容错解析 + 单位标准化 + 枚举校验 + 图片哈希缓存 |
| `src/vision/integration.py` | 合并进 RequirementProfile；冲突检测；尺寸只当 hint；指标 |
| `tests/test_vision.py` | 50 个单元测试（解析 / 单位 / 来源 / 缓存 / 合并 / Gate / 降级） |
| `tests/test_vision_pipeline.py` | 13 个端到端测试（纯图片 / 图文 / 冲突 / 失败 / 首轮接待 / API 入参） |
| `tests/vision_golden/` | Golden Dataset：生成脚本 + 8 张图片 + manifest + 评测脚本 + 报告 |

## 修改

| 文件 | 改动 |
| --- | --- |
| `src/config.py` | 新增 `GLM_API_KEY / GLM_VISION_MODEL / GLM_API_BASE / VISION_ENABLED / VISION_TIMEOUT_SECS / VISION_MAX_RETRIES / VISION_MAX_IMAGES / VISION_MAX_IMAGE_MB` |
| `src/models/requirement.py` | 来源强度加入 `vision_explicit(5) / vision_inferred(3)`；判定"图片明确可见"可信字段 `VISION_TRUSTED_FIELDS`；新增 `conflict_slots` / `vision_size_hint_mm` / `vision_notes` |
| `src/rag/readiness.py` | Gate 认可 `vision_explicit` 的环境/场景/安装方式/屏类型；冲突时改问冲突项；问尺寸时带上"图片估计值" |
| `src/orchestrator.py` | `process_message(..., images=...)`：纯图片消息处理、Vision 提取、合并写回 memory、指标日志 |
| `src/api.py` | `ChatRequest.images`（url / data + mime_type）、`_normalize_images()`、响应 `vision` 字段、SSE 同步支持 |
| `static/index.html` / `static/app.js` / `static/style.css` | 输入框左侧「图片」按钮（上传）、**全局粘贴**（Ctrl+V，不要求输入框聚焦）、拖拽上传、缩略图预览、随消息发送；静态资源带 `?v=` 版本号避免浏览器用旧 JS |
| `.env.example` | 新增智谱视觉相关配置项与说明 |

---

# 附录 B：实测结果（2026-09-16）

## B1 接口实测

```text
模型：glm-4v-plus（智谱开放平台）
输入：tests/vision_golden/images/indoor_conference_1.png
输出：display_type=LED(vision_explicit) environment=indoor(vision_explicit)
      purpose=conference(vision_explicit) 尺寸=null（未编造）
耗时：实测约 1~7 秒/张（640×480 的合成图约 6.8 秒；越小越快）
```

## B2 Golden Dataset 基线（8 张合成图，`tests/vision_golden/last_report.json`）

| 指标 | 结果 | 目标 | 说明 |
| --- | --- | --- | --- |
| display_type_accuracy | 1.000 | ≥0.90 | 8/8 |
| environment_accuracy | 0.875 | ≥0.95 | 唯一失败：无文字标注的合成户外图被判成室内 |
| purpose_accuracy | 1.000 | — | |
| installation_accuracy | 1.000 | — | 租赁舞台图识别出 rental |
| null_precision | 1.000 | — | 没有屏幕的图片一个字段都没填 |
| clear_size_accuracy | 1.000 | — | 图纸上的 5000mm × 3000mm 读出来了 |
| size_fabrication_rate | 0.000 | ≤0.05 | 没有尺寸的图片不编尺寸 |
| vision_inferred_as_confirmed | 0 | =0 | 图片尺寸只进 hint，工程字段始终为空 |
| Vision 失败导致主流程失败 | 0 | =0 | 失败降级有测试覆盖 |
| 同图重复调用 | 0 | =0 | 会话内图片哈希缓存 |

## B3 回归测试

```text
python -m pytest tests -q
→ 834 passed, 4 skipped
```

---

# 附录 C：已知限制与后续建议

1. **Golden Dataset 目前是合成图**：只能验证链路与"是否会瞎编"。
   计划里要求的 50 张真实（脱敏）客户照片需要业务侧提供后替换
   `tests/vision_golden/images/` 并更新 `manifest.json`，才能得到有意义的精度基线。
2. **模型名**：默认 `glm-4v-plus`，可用 `GLM_VISION_MODEL` 覆盖；
   若账号未开通该模型，会自动按 `glm-4v-plus → glm-4.5v → glm-4v` 回退。
3. **图片尺寸永远是提示**：即使图片里明确写了尺寸（如图纸标注），也只写入
   `vision_size_hint_mm` 用于向客户确认；最终工程计算仍然只接受客户确认的尺寸。
   如果业务上希望"图纸标注尺寸可直接用于计算"，需要单独评估（当前按计划第十八阶段从严处理）。
4. **多图**：一次最多 3 张（`VISION_MAX_IMAGES`），多张按"显式优先于推断"合并。
5. **性能**：视觉调用实测约 1~7 秒/张（合成图 6.8s），会叠加在首轮延迟上；
   当前是串行调用（最多 3 张），必要时可改为并行或把视觉提取异步化。
6. **真实链路已实测**：
   ```text
   第1轮（只发图片）：Vision 提取 LED/indoor/conference → 首轮接待照常执行 → 需求入库
   第2轮（补一句文字）：AI 没有再问"室内还是室外"，直接问"固定安装还是租赁"
   ```
